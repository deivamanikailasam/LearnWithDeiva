{
  "snippets": [
    {
      "title": "Functional updates",
      "language": "jsx",
      "code": "// ❌ may use stale state in rapid updates\nsetCount(count + 1);\n\n// ✅ always uses the latest value\nsetCount((c) => c + 1);",
      "explanation": "Prefer the updater function when the new value depends on the old one."
    },
    {
      "title": "Updating objects immutably",
      "language": "jsx",
      "code": "const [user, setUser] = useState({ name: 'Ada', age: 36 });\n\n// create a new object instead of mutating\nsetUser((u) => ({ ...u, age: u.age + 1 }));",
      "explanation": "Spread the old state into a new object so React detects the change."
    }
  ]
}
