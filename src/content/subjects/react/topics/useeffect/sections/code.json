{
  "snippets": [
    {
      "title": "Fetch on mount with cleanup",
      "language": "jsx",
      "code": "useEffect(() => {\n  let active = true;\n  fetch('/api/user')\n    .then((r) => r.json())\n    .then((data) => {\n      if (active) setUser(data);\n    });\n  return () => {\n    active = false; // ignore the response if unmounted\n  };\n}, []);",
      "explanation": "The active flag avoids setting state on an unmounted component."
    }
  ]
}
