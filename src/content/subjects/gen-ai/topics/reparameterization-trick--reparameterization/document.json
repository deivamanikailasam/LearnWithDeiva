{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "The Core Idea" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The reparametrisation trick transforms sampling from a parameterised distribution into a two-step process: first sample a fixed noise variable ε from a parameter-free base distribution p(ε), then apply a deterministic transformation that depends on the parameters. This separates randomness from parameters, making gradient flow through sampling possible."
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "z \\sim q_\\phi(z) \\quad \\Longleftrightarrow \\quad \\varepsilon \\sim p(\\varepsilon),\\quad z = g_\\phi(\\varepsilon)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Once z is written as g_φ(ε), the chain rule applies: ∂L/∂φ = (∂L/∂z)(∂z/∂φ). Autograd frameworks (PyTorch, JAX) handle the rest automatically."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Gaussian Reparametrisation" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The most common case: q_φ(z|x) = N(μ, σ²I). Drawing z ~ N(μ, σ²I) is equivalent to:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "\\varepsilon \\sim \\mathcal{N}(0, I), \\quad z = \\mu + \\sigma \\odot \\varepsilon"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The gradient with respect to μ and σ is clean: ∂z/∂μ = 1 and ∂z/∂σ = ε. In practice, networks often output log σ² (log variance) rather than σ for numerical stability, so σ = exp(½ log σ²)."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": { "language": "python" },
        "content": [
          {
            "type": "text",
            "text": "import torch\nimport torch.nn as nn\n\nclass GaussianReparamSample(nn.Module):\n    \"\"\"\n    Reparametrised sample from N(mu, exp(log_var)) — differentiable.\n    \"\"\"\n    def forward(self, mu, log_var):\n        std = torch.exp(0.5 * log_var)   # σ from log σ²\n        eps = torch.randn_like(std)       # ε ~ N(0, I)\n        return mu + std * eps             # z = μ + σ ⊙ ε\n\n# Verify gradients flow back to mu and log_var\nsampler = GaussianReparamSample()\nmu      = torch.zeros(4, requires_grad=True)\nlog_var = torch.zeros(4, requires_grad=True)\nz = sampler(mu, log_var)\nloss = z.sum()\nloss.backward()\nprint(\"grad mu:\",      mu.grad)       # all ones (∂z/∂μ = 1)\nprint(\"grad log_var:\", log_var.grad)  # 0.5 * eps (∂z/∂log_var = 0.5 * σ * eps)"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Other Location-Scale Families" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Any distribution in the location-scale family — where z ~ q(μ, σ) can be written as z = μ + σ ε with ε from a fixed base distribution — admits this trick directly. Common examples:"
          }
        ]
      },
      {
        "type": "table",
        "content": [
          {
            "type": "tableRow",
            "content": [
              { "type": "tableHeader", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "Distribution" }] }] },
              { "type": "tableHeader", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "Base noise ε" }] }] },
              { "type": "tableHeader", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "Transform g(φ, ε)" }] }] }
            ]
          },
          {
            "type": "tableRow",
            "content": [
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "Gaussian N(μ, σ²)" }] }] },
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "N(0, 1)" }] }] },
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "marks": [{ "type": "code" }], "text": "μ + σ * ε" }] }] }
            ]
          },
          {
            "type": "tableRow",
            "content": [
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "Laplace(μ, b)" }] }] },
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "Uniform(0,1)" }] }] },
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "marks": [{ "type": "code" }], "text": "μ - b * sign(u-0.5) * log(1-2|u-0.5|)" }] }] }
            ]
          },
          {
            "type": "tableRow",
            "content": [
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "Exponential(λ)" }] }] },
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "Uniform(0,1)" }] }] },
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "marks": [{ "type": "code" }], "text": "-log(u) / λ" }] }] }
            ]
          },
          {
            "type": "tableRow",
            "content": [
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "Gumbel(μ, β)" }] }] },
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "Uniform(0,1)" }] }] },
              { "type": "tableCell", "content": [{ "type": "paragraph", "content": [{ "type": "text", "marks": [{ "type": "code" }], "text": "μ - β * log(-log(u))" }] }] }
            ]
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Gumbel-Softmax for Discrete Variables" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The reparametrisation trick does not directly apply to discrete distributions since sampling from a categorical is not differentiable. The Gumbel-Softmax (Concrete) trick provides a continuous relaxation: a categorical with logits π = (π₁, …, πK) can be approximated by:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "y_k = \\frac{\\exp((\\log \\pi_k + g_k) / \\tau)}{\\sum_{j=1}^K \\exp((\\log \\pi_j + g_j) / \\tau)}, \\quad g_k \\sim \\text{Gumbel}(0,1)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "where τ is the temperature. As τ→0, the samples approach one-hot vectors (true categorical). At τ=1 the distribution is smooth and fully differentiable. In practice use a straight-through estimator or anneal τ from 1 to a small value during training."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": { "language": "python" },
        "content": [
          {
            "type": "text",
            "text": "import torch\nimport torch.nn.functional as F\n\ndef gumbel_softmax(logits, tau=1.0, hard=False):\n    \"\"\"\n    Differentiable sample from Categorical(softmax(logits)).\n    hard=True: straight-through estimator (one-hot forward, soft backward).\n    \"\"\"\n    # Sample Gumbel noise\n    u = torch.zeros_like(logits).uniform_().clamp_(1e-20, 1)\n    g = -torch.log(-torch.log(u))  # ~ Gumbel(0,1)\n    y_soft = F.softmax((logits + g) / tau, dim=-1)\n    if not hard:\n        return y_soft\n    # Straight-through: one-hot forward, soft backward\n    k     = y_soft.argmax(dim=-1)\n    y_hard = torch.zeros_like(logits).scatter_(-1, k.unsqueeze(-1), 1.0)\n    return (y_hard - y_soft).detach() + y_soft\n\n# Example: 4-way categorical\nlogits = torch.tensor([1.0, 2.0, 0.5, -1.0], requires_grad=True)\ny = gumbel_softmax(logits, tau=0.5, hard=True)\nprint(y)                        # one-hot, e.g. [0, 1, 0, 0]\ny.sum().backward()              # gradients still flow to logits\nprint(logits.grad)              # non-zero"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Multi-Sample Objectives" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "A single reparametrised sample gives an unbiased but noisy gradient estimate. Using L > 1 samples reduces variance proportionally to 1/L at L times the compute:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "\\hat{\\nabla}_\\phi \\mathcal{L} = \\frac{1}{L} \\sum_{\\ell=1}^{L} \\nabla_\\phi f(g_\\phi(\\varepsilon^{(\\ell)}))"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The IWAE (Importance Weighted Autoencoder) uses L samples to construct a tighter lower bound than the standard ELBO. Each additional sample refines the estimate of the log-marginal but at the cost of more forward passes through the decoder."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": { "language": "python" },
        "content": [
          {
            "type": "text",
            "text": "def iwae_elbo(x, encoder, decoder, L=5):\n    \"\"\"\n    Importance-weighted ELBO with L samples.\n    Tighter bound than standard ELBO; approaches log p(x) as L → ∞.\n    \"\"\"\n    mu, log_var = encoder(x)              # (batch, z_dim)\n    std = torch.exp(0.5 * log_var)\n\n    # Draw L samples via reparametrisation\n    eps = torch.randn(L, *mu.shape)       # (L, batch, z_dim)\n    z   = mu.unsqueeze(0) + std.unsqueeze(0) * eps  # (L, batch, z_dim)\n\n    # Importance weights: log p(x|z) + log p(z) - log q(z|x)\n    log_p_xz = decoder(z.view(L * len(x), -1)).view(L, len(x))  # (L, batch)\n    log_p_z  = -0.5 * (z**2 + math.log(2 * math.pi)).sum(-1)   # (L, batch)\n    log_q_z  = -0.5 * ((z - mu)**2 / std**2 + log_var + math.log(2*math.pi)).sum(-1)\n\n    log_w  = log_p_xz + log_p_z - log_q_z  # (L, batch)\n    # IWAE objective: mean over batch of log mean over L samples\n    elbo = torch.logsumexp(log_w, dim=0) - math.log(L)\n    return elbo.mean()"
          }
        ]
      }
    ]
  }
}
