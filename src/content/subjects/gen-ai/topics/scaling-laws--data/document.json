{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Training data is the second axis of the scaling laws alongside model parameters. The Kaplan et al. (2020) laws showed that loss decreases as a power law in token count, and that for a fixed compute budget, the optimal strategy is to scale data and parameters together. The Hoffmann et al. (2022) \"Chinchilla\" paper refined this: a model trained on roughly 20 tokens per parameter reaches the compute-optimal frontier. Under-training large models on too little data is wasteful — a 70B model trained on 1T tokens is suboptimal; at 1.4T tokens (20× parameters) it is compute-optimal."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "The Chinchilla Scaling Law"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The Chinchilla paper fit a loss function over (N, D) — parameter count and token count — to determine the compute-optimal allocation for a given FLOPs budget "
          },
          {
            "type": "text",
            "marks": [{"type": "code"}],
            "text": "C ≈ 6ND"
          },
          {
            "type": "text",
            "text": ":"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "N_{\\text{opt}} \\propto C^{0.5}, \\quad D_{\\text{opt}} \\propto C^{0.5} \\quad \\Rightarrow \\quad D_{\\text{opt}} \\approx 20 \\cdot N_{\\text{opt}}"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "This predicts that compute should be split roughly equally between parameters and data. Earlier models like GPT-3 (175B parameters, 300B tokens ≈ 1.7 tokens/param) were significantly under-trained by this criterion. Chinchilla (70B parameters, 1.4T tokens = 20 tokens/param) achieved better benchmark performance despite being 2.5× smaller than GPT-3."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Computing Optimal Token Count"}]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "def chinchilla_optimal(\n    compute_flops: float | None = None,\n    n_params: float | None = None,\n    tokens: float | None = None,\n    flops_per_token_per_param: float = 6.0,\n) -> dict:\n    \"\"\"\n    Given any two of (compute, parameters, tokens), compute the third\n    and the optimal split according to Chinchilla scaling.\n\n    flops_per_token_per_param: ~6 for a standard transformer forward+backward pass.\n    \"\"\"\n    if compute_flops is not None and n_params is not None:\n        tokens = compute_flops / (flops_per_token_per_param * n_params)\n        return {\"tokens\": tokens, \"tokens_per_param\": tokens / n_params,\n                \"compute_flops\": compute_flops, \"n_params\": n_params}\n    elif compute_flops is not None and tokens is not None:\n        n_params = compute_flops / (flops_per_token_per_param * tokens)\n        return {\"n_params\": n_params, \"tokens_per_param\": tokens / n_params,\n                \"compute_flops\": compute_flops, \"tokens\": tokens}\n    elif n_params is not None and tokens is not None:\n        compute_flops = flops_per_token_per_param * n_params * tokens\n        return {\"compute_flops\": compute_flops, \"tokens_per_param\": tokens / n_params,\n                \"n_params\": n_params, \"tokens\": tokens}\n    else:\n        raise ValueError(\"Provide exactly two of: compute_flops, n_params, tokens\")\n\n\n# Optimal data given a fixed model size\nfor n_b in [1, 7, 13, 70, 405]:   # billions of parameters\n    n = n_b * 1e9\n    optimal_tokens = 20 * n        # Chinchilla: 20 tokens per parameter\n    result = chinchilla_optimal(n_params=n, tokens=optimal_tokens)\n    print(f\"{n_b:>4}B params → {optimal_tokens/1e9:.0f}B tokens  \"\n          f\"(compute = {result['compute_flops']:.2e} FLOPs)\")\n\n#    1B params →  20B tokens  (compute = 1.20e+11 FLOPs)\n#    7B params → 140B tokens  (compute = 8.40e+12 FLOPs)\n#   13B params → 260B tokens  (compute = 1.56e+13 FLOPs)\n#   70B params → 1400B tokens (compute = 8.40e+14 FLOPs)\n#  405B params → 8100B tokens (compute = 4.86e+15 FLOPs)\n\n# Real models vs Chinchilla-optimal\nprint(\"\\nReal models vs Chinchilla-optimal:\")\nmodels = [\n    # name,          n_params_B, tokens_B\n    (\"GPT-3\",        175,   300),\n    (\"Chinchilla\",    70,  1400),\n    (\"LLaMA 1 65B\",  65,  1400),\n    (\"LLaMA 3 8B\",    8, 15000),   # ~1875 tokens/param — deliberately over-trained\n    (\"LLaMA 3 70B\",  70, 15000),   # ~214 tokens/param\n]\nfor name, n_b, d_b in models:\n    ratio = d_b / n_b\n    status = \"over-trained\" if ratio > 30 else (\"under-trained\" if ratio < 15 else \"near-optimal\")\n    print(f\"  {name:<18} {n_b:>4}B params, {d_b:>5}B tokens, {ratio:.0f} tok/param — {status}\")\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Data Quality vs Quantity"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Raw token count is not the only metric. The composition and quality of the training corpus matter significantly. Models like LLaMA 3 deliberately exceed the Chinchilla-optimal token count (15T tokens for a 70B model = 214 tokens/param) because they prioritise the resulting "
          },
          {
            "type": "text",
            "marks": [{"type": "bold"}],
            "text": "inference-time efficiency"
          },
          {
            "type": "text",
            "text": ": an over-trained smaller model beats a Chinchilla-optimal larger model at the same inference cost. This is the \"inference-compute-optimal\" regime."
          }
        ]
      },
      {
        "type": "table",
        "content": [
          {
            "type": "tableRow",
            "content": [
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Data factor"}]}]},
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Effect on model quality"}]}]},
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Common practice"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Deduplication"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Removing near-duplicates reduces memorisation and improves generalisation"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "MinHash LSH or exact n-gram dedup"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Domain mix"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "More code → better coding; more books → better reasoning"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Domain weights tuned on downstream evals"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Quality filtering"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Removing low-quality web pages improves all benchmarks"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Perplexity filter, n-gram overlap with reference corpus"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Multilingual coverage"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "More tokens in a language → better generation in that language"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Oversample low-resource languages to balance"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Epoch repetition"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Repeating data beyond 1 epoch degrades generalization; <4× repetition usually acceptable"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Avoid with large enough unique datasets"}]}]}
            ]
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Estimating Dataset Size"}]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "import tiktoken\n\nenc = tiktoken.get_encoding(\"cl100k_base\")   # GPT-4 / LLaMA tokenizer (approximate)\n\ndef estimate_tokens(text: str) -> int:\n    return len(enc.encode(text))\n\n# Typical token densities for common sources\nSOURCES = {\n    \"Web page (average)\": 500,        # tokens\n    \"News article\": 800,\n    \"Book (average, 80K words)\": 110_000,\n    \"ArXiv paper\": 8_000,\n    \"GitHub file (Python, average)\": 400,\n    \"Wikipedia article\": 1_500,\n}\n\nprint(\"Token budget for a 7B Chinchilla-optimal run: 140B tokens\")\nprint(\"That's roughly:\")\nfor source, toks in SOURCES.items():\n    count = 140_000_000_000 / toks\n    print(f\"  {count:>15,.0f}  {source}\")\n\n# Measure actual density on your data\nsample_text = \"\"\"The transformer architecture revolutionised natural language processing.\n    Self-attention allows every token to attend to every other token in the sequence.\"\"\"\nprint(f\"\\nSample text: {estimate_tokens(sample_text)} tokens for {len(sample_text.split())} words\")\nprint(f\"Words-per-token ratio: {len(sample_text.split()) / estimate_tokens(sample_text):.2f}\")\n# Typical English: ~0.75 words per token (1.33 tokens per word)\n"
          }
        ]
      },
      {
        "type": "blockquote",
        "content": [
          {
            "type": "paragraph",
            "content": [
              {
                "type": "text",
                "text": "The Chinchilla result shifted the field from \"bigger model = better\" to \"right-sized model on right-sized data\". But the story keeps evolving: LLaMA 3's decision to train the 8B model on 15T tokens (1875 tokens/param, far beyond Chinchilla-optimal) showed that over-training a smaller model is often the right choice when serving cost matters more than training cost. The practical takeaway is that data quantity and quality are both first-class levers — not afterthoughts to be filled with whatever web crawl is available."
              }
            ]
          }
        ]
      }
    ]
  }
}
