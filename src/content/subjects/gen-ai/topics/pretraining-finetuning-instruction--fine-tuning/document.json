{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Fine-tuning takes a pretrained base model and continues training it on a smaller, task-specific dataset with labelled examples. Unlike pretraining — which uses raw text with no labels — fine-tuning requires (input, output) pairs and uses supervised cross-entropy loss. The goal is to specialise the model's behaviour for a target task or domain while retaining the broad knowledge acquired during pretraining. Fine-tuning is the workhorse of practical LLM deployment: it is how a general-purpose base model becomes a customer support bot, a code completion engine, or a clinical note summariser."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Full Fine-Tuning"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "All model parameters are updated on the task dataset. This gives maximum adaptability but requires the same memory as pretraining — which is impractical for 7B+ parameter models on consumer hardware. Full fine-tuning also risks catastrophic forgetting: the model over-adapts to the small task dataset and loses generalisation ability."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "from datasets import Dataset\nfrom transformers import (\n    AutoTokenizer, AutoModelForSequenceClassification,\n    TrainingArguments, Trainer\n)\nimport torch\n\n\ndef full_fine_tune_classifier(\n    base_model: str,\n    train_texts: list[str],\n    train_labels: list[int],\n    num_labels: int,\n    num_epochs: int = 3,\n    lr: float = 2e-5,\n):\n    \"\"\"\n    Full fine-tuning for text classification.\n    All parameters are updated — appropriate for small models (<3B)\n    or when you have a large domain-specific dataset.\n    \"\"\"\n    tokenizer = AutoTokenizer.from_pretrained(base_model)\n    model = AutoModelForSequenceClassification.from_pretrained(\n        base_model, num_labels=num_labels\n    )\n\n    def preprocess(examples):\n        return tokenizer(\n            examples[\"text\"],\n            truncation=True, max_length=512, padding=\"max_length\"\n        )\n\n    dataset = Dataset.from_dict({\"text\": train_texts, \"label\": train_labels})\n    dataset = dataset.map(preprocess, batched=True)\n\n    args = TrainingArguments(\n        output_dir=\"./fine-tuned\",\n        num_train_epochs=num_epochs,\n        per_device_train_batch_size=16,\n        learning_rate=lr,\n        warmup_ratio=0.06,\n        weight_decay=0.01,\n        bf16=True,\n        evaluation_strategy=\"epoch\",\n        save_strategy=\"epoch\",\n        load_best_model_at_end=True,\n    )\n    Trainer(\n        model=model,\n        args=args,\n        train_dataset=dataset,\n    ).train()\n    return model\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Parameter-Efficient Fine-Tuning: LoRA"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "LoRA (Low-Rank Adaptation, Hu et al. 2022) freezes the pretrained weights and introduces trainable low-rank decomposition matrices into specific linear layers. Only those matrices are updated — typically less than 1% of total parameters — achieving nearly the same task performance as full fine-tuning at a fraction of the compute and memory cost."
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "h = W_0 x + \\Delta W x = W_0 x + \\frac{\\alpha}{r} B A x, \\quad A \\in \\mathbb{R}^{r \\times d_{\\text{in}}},\\; B \\in \\mathbb{R}^{d_{\\text{out}} \\times r}"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "A is initialised with random Gaussian values; B is initialised to zero so ΔW = 0 at the start of training. The scaling factor α/r is a hyperparameter. At inference, BA can be merged into W₀ with no additional latency."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "import torch\nfrom transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments, Trainer\nfrom peft import LoraConfig, get_peft_model, TaskType\nfrom datasets import Dataset\n\n\ndef lora_fine_tune_causal_lm(\n    base_model: str,\n    instruction_response_pairs: list[dict],  # [{\"instruction\": ..., \"output\": ...}]\n    r: int = 16,\n    lora_alpha: int = 32,\n    target_modules: list[str] | None = None,\n):\n    \"\"\"\n    LoRA fine-tuning for causal language models.\n    target_modules: which linear layers get LoRA adapters.\n    Defaults to query and value projections.\n    \"\"\"\n    if target_modules is None:\n        target_modules = [\"q_proj\", \"v_proj\"]\n\n    tokenizer = AutoTokenizer.from_pretrained(base_model)\n    tokenizer.pad_token = tokenizer.eos_token\n\n    model = AutoModelForCausalLM.from_pretrained(\n        base_model, torch_dtype=torch.bfloat16, device_map=\"auto\"\n    )\n\n    lora_config = LoraConfig(\n        task_type=TaskType.CAUSAL_LM,\n        r=r,\n        lora_alpha=lora_alpha,\n        target_modules=target_modules,\n        lora_dropout=0.05,\n        bias=\"none\",\n    )\n    model = get_peft_model(model, lora_config)\n    model.print_trainable_parameters()\n\n    # Format as instruction-following sequences\n    def format_and_tokenise(example):\n        text = (\n            f\"### Instruction:\\n{example['instruction']}\\n\"\n            f\"### Response:\\n{example['output']}{tokenizer.eos_token}\"\n        )\n        tokens = tokenizer(text, truncation=True, max_length=1024, padding=\"max_length\")\n        tokens[\"labels\"] = tokens[\"input_ids\"].copy()\n        return tokens\n\n    dataset = Dataset.from_list(instruction_response_pairs)\n    dataset = dataset.map(format_and_tokenise)\n\n    args = TrainingArguments(\n        output_dir=\"./lora-output\",\n        per_device_train_batch_size=4,\n        gradient_accumulation_steps=4,\n        num_train_epochs=3,\n        learning_rate=3e-4,   # LoRA can use higher lr than full fine-tuning\n        bf16=True,\n        logging_steps=10,\n        save_steps=100,\n    )\n    Trainer(model=model, args=args, train_dataset=dataset).train()\n\n    # Merge adapters for zero-overhead inference\n    merged = model.merge_and_unload()\n    return merged\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "When Fine-Tuning Helps and When It Doesn't"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Fine-tuning is often applied prematurely. Before committing to it, always test whether prompting alone achieves the goal — it is far cheaper to iterate on prompts than to curate a labelled dataset and run a training job."
          }
        ]
      },
      {
        "type": "table",
        "content": [
          {
            "type": "tableRow",
            "content": [
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Fine-tune when…"}]}]},
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Prefer prompting when…"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "You need a specific output format the model never produces zero-shot"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "The task is within the model's general capabilities"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "The domain uses terminology absent from pretraining data"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "You have fewer than ~1 000 labelled examples"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "You need consistent tone, persona, or output length"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Requirements are changing quickly — re-curating data is costly"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Latency requires a smaller model that outperforms a larger zero-shot one"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "You need knowledge from documents not in the training set (use RAG instead)"}]}]}
            ]
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Preventing Catastrophic Forgetting"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "When fine-tuning on a small dataset, the model can overfit to the task distribution and lose performance on general tasks. Key mitigations:"
          }
        ]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [
              {"type": "text", "marks": [{"type": "bold"}], "text": "Use LoRA. "},
              {"type": "text", "text": "Frozen base weights cannot be forgotten. The adapter learns the delta while the original knowledge stays intact."}
            ]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [
              {"type": "text", "marks": [{"type": "bold"}], "text": "Mix in pretraining data. "},
              {"type": "text", "text": "Include a small fraction (~5–10%) of general text from the pretraining corpus in the fine-tuning batch. This replays prior knowledge and resists forgetting."}
            ]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [
              {"type": "text", "marks": [{"type": "bold"}], "text": "Lower the learning rate. "},
              {"type": "text", "text": "Fine-tuning learning rates (1e-5 to 5e-5 for full fine-tuning) are typically 10–100× smaller than pretraining rates to avoid large parameter shifts."}
            ]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [
              {"type": "text", "marks": [{"type": "bold"}], "text": "Early stopping. "},
              {"type": "text", "text": "Monitor held-out task performance and stop before the model over-adapts. Validation loss on the task set typically bottoms out within 1–3 epochs."}
            ]}]
          }
        ]
      },
      {
        "type": "blockquote",
        "content": [
          {
            "type": "paragraph",
            "content": [
              {
                "type": "text",
                "text": "Fine-tuning teaches a model how to behave; pretraining taught it what to know. The distinction matters: fine-tuning on labelled examples cannot implant new factual knowledge not in the base model — for that, you need either pretraining on the new data or retrieval augmentation at inference time."
              }
            ]
          }
        ]
      }
    ]
  }
}
