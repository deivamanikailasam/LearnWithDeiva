{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Before transformers dominated language modelling, two families of autoregressive models competed: n-gram models (count-based) and recurrent neural language models (RNN-LMs). Both share the same objective — maximise the likelihood of a sequence by predicting each token given all prior tokens — but differ in how context is represented. Understanding these pre-transformer models clarifies exactly what transformers improved upon and why perplexity is the standard evaluation metric."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "The Language Modelling Objective"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "A language model assigns a probability to every sequence of tokens. Using the chain rule of probability:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "p(x_1, x_2, \\ldots, x_T) = \\prod_{t=1}^{T} p(x_t \\mid x_1, \\ldots, x_{t-1})"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Training minimises the negative log-likelihood (cross-entropy loss) over a corpus. Perplexity (PPL) measures how surprised the model is per token — lower is better:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "\\text{PPL} = \\exp\\!\\left(-\\frac{1}{T}\\sum_{t=1}^{T}\\log p(x_t \\mid x_{<t})\\right)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "A model with PPL=10 is roughly as uncertain as if it were choosing uniformly among 10 equiprobable alternatives at each step. On Penn Treebank, n-gram models achieved ~140 PPL; good LSTM-LMs reached ~55–70; transformers reached ~20–30."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "N-Gram Language Models"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "N-gram models truncate the context to the last "
          },
          {
            "type": "text",
            "marks": [{"type": "italic"}],
            "text": "n-1"
          },
          {
            "type": "text",
            "text": " tokens: "
          },
          {
            "type": "text",
            "marks": [{"type": "italic"}],
            "text": "p(x_t | x_{t-n+1}, ..., x_{t-1})"
          },
          {
            "type": "text",
            "text": ". Probabilities are estimated from corpus counts with Laplace or Kneser-Ney smoothing to handle zero-count n-grams. The fundamental limitation is sparsity: longer contexts (larger n) lead to exponentially more n-grams, most of which never appear in training data."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "from collections import defaultdict, Counter\nimport math\n\n\nclass NgramLM:\n    \"\"\"Simple n-gram language model with add-k (Laplace) smoothing.\"\"\"\n\n    def __init__(self, n: int = 3, k: float = 0.1):\n        self.n = n\n        self.k = k          # smoothing constant\n        self.counts = defaultdict(Counter)   # context -> {next_token: count}\n        self.vocab = set()\n\n    def train(self, tokens: list):\n        self.vocab.update(tokens)\n        V = len(self.vocab)\n        # Count n-grams\n        for i in range(len(tokens) - self.n + 1):\n            ctx = tuple(tokens[i:i + self.n - 1])   # context\n            nxt = tokens[i + self.n - 1]             # next token\n            self.counts[ctx][nxt] += 1\n\n    def log_prob(self, context: tuple, token: str) -> float:\n        \"\"\"log p(token | context) with Laplace smoothing.\"\"\"\n        ctx = tuple(context[-(self.n - 1):])   # trim to n-1\n        count_nxt = self.counts[ctx].get(token, 0) + self.k\n        total = sum(self.counts[ctx].values()) + self.k * len(self.vocab)\n        return math.log(count_nxt / total)\n\n    def perplexity(self, tokens: list) -> float:\n        \"\"\"Compute perplexity on a token sequence.\"\"\"\n        log_prob_sum = 0.0\n        T = len(tokens) - self.n + 1\n        for i in range(T):\n            ctx = tuple(tokens[i:i + self.n - 1])\n            log_prob_sum += self.log_prob(ctx, tokens[i + self.n - 1])\n        return math.exp(-log_prob_sum / T)"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Character-Level vs. Word-Level"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The choice of tokenisation unit determines the model's vocabulary size, sequence length, and the kinds of patterns it must learn:"
          }
        ]
      },
      {
        "type": "table",
        "content": [
          {
            "type": "tableRow",
            "content": [
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Property"}]}]},
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Character-level"}]}]},
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Word-level"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Vocabulary size"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "~100–300 (Unicode blocks)"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "10k–200k (open vocab)"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "OOV problem"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "None (all chars seen)"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Common (rare words → <UNK>)"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Sequence length"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Long (chars × tokens)"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Short"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Morphology"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Must learn from characters"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Embedded per-word, loses structure"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Long-range dependency difficulty"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Higher (more steps to span a sentence)"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Lower (word = natural unit)"}]}]}
            ]
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "LSTM Language Model"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "An LSTM-based LM (Mikolov et al., 2010 for RNN; Sundermeyer et al., 2012 for LSTM) replaces the fixed n-gram context with a recurrent hidden state that theoretically summarises the entire history. Each token is embedded, passed through one or more LSTM layers, and the output is projected to a softmax over the vocabulary."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\nclass CharLM(nn.Module):\n    \"\"\"\n    Character-level LSTM language model.\n    Input: integer token indices in {0, ..., vocab_size-1}.\n    Output: logits over vocabulary for each position.\n    \"\"\"\n\n    def __init__(\n        self,\n        vocab_size: int,\n        emb_dim: int = 128,\n        hidden_dim: int = 512,\n        n_layers: int = 2,\n        dropout: float = 0.5,\n    ):\n        super().__init__()\n        self.embedding = nn.Embedding(vocab_size, emb_dim)\n        self.lstm = nn.LSTM(\n            emb_dim, hidden_dim, n_layers,\n            batch_first=True, dropout=dropout if n_layers > 1 else 0.0,\n        )\n        self.drop = nn.Dropout(dropout)\n        self.proj = nn.Linear(hidden_dim, vocab_size)\n        # Weight tying: output projection shares weights with embedding\n        self.proj.weight = self.embedding.weight\n        self.hidden_dim = hidden_dim\n        self.n_layers = n_layers\n\n    def forward(\n        self,\n        x: torch.Tensor,    # [B, T] integer token indices\n        hidden=None,\n    ):\n        emb = self.drop(self.embedding(x))  # [B, T, emb_dim]\n        out, hidden = self.lstm(emb, hidden) # [B, T, hidden_dim]\n        logits = self.proj(self.drop(out))   # [B, T, vocab_size]\n        return logits, hidden\n\n    def init_hidden(self, batch_size: int, device):\n        h = torch.zeros(self.n_layers, batch_size, self.hidden_dim, device=device)\n        c = torch.zeros(self.n_layers, batch_size, self.hidden_dim, device=device)\n        return h, c\n\n\ndef lm_loss(\n    model: CharLM,\n    x: torch.Tensor,   # [B, T+1] token sequences (input and target offset by 1)\n) -> torch.Tensor:\n    \"\"\"\n    Teacher-forcing cross-entropy loss.\n    Input  x[:, :-1] (tokens 0..T-1), targets x[:, 1:] (tokens 1..T).\n    \"\"\"\n    inp = x[:, :-1]      # [B, T]\n    target = x[:, 1:]    # [B, T]\n    logits, _ = model(inp)  # [B, T, V]\n    B, T, V = logits.shape\n    return F.cross_entropy(logits.reshape(B * T, V), target.reshape(B * T))"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Weight Tying"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Weight tying (Press & Wolf, 2017) shares the embedding matrix with the output projection. Intuitively, both layers relate words to the same latent semantic space — the embedding maps tokens to vectors, and the output projection maps vectors back to token probabilities using the same geometry. Tying reduces parameters substantially (by "
          },
          {
            "type": "text",
            "marks": [{"type": "italic"}],
            "text": "V × emb_dim"
          },
          {
            "type": "text",
            "text": ") and consistently improves perplexity."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Generating Text"}]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "def compute_perplexity(\n    model: CharLM,\n    tokens: torch.Tensor,   # [1, T+1]\n    device: torch.device,\n) -> float:\n    \"\"\"Evaluate per-token perplexity on a sequence.\"\"\"\n    model.eval()\n    with torch.no_grad():\n        loss = lm_loss(model, tokens.to(device))\n    return loss.exp().item()\n\n\ndef sample_text(\n    model: CharLM,\n    seed_tokens: list,          # list of initial token indices\n    idx_to_char: dict,\n    n_chars: int = 200,\n    temperature: float = 1.0,\n    top_k: int = 0,             # 0 = no top-k filtering\n    device: torch.device = torch.device('cpu'),\n) -> str:\n    \"\"\"\n    Autoregressively sample characters from the LM.\n    temperature < 1: sharper (more deterministic)\n    temperature > 1: flatter (more random)\n    top_k > 0: only sample from top-k most probable tokens\n    \"\"\"\n    model.eval()\n    x = torch.tensor(seed_tokens, dtype=torch.long, device=device).unsqueeze(0)  # [1, L]\n    hidden = model.init_hidden(1, device)\n    generated = list(seed_tokens)\n\n    with torch.no_grad():\n        for _ in range(n_chars):\n            logits, hidden = model(x, hidden)    # [1, 1, V]\n            logits = logits[0, -1, :]            # [V]\n            logits = logits / temperature\n            if top_k > 0:\n                topk_vals, _ = torch.topk(logits, top_k)\n                threshold = topk_vals[-1]\n                logits[logits < threshold] = float('-inf')\n            probs = torch.softmax(logits, dim=-1)\n            next_token = torch.multinomial(probs, 1).item()\n            generated.append(next_token)\n            x = torch.tensor([[next_token]], dtype=torch.long, device=device)\n\n    return ''.join(idx_to_char.get(t, '?') for t in generated)\n\n\ndef nucleus_sample(\n    logits: torch.Tensor,\n    p: float = 0.9,\n) -> int:\n    \"\"\"\n    Top-p (nucleus) sampling: sample from the smallest set of tokens\n    whose cumulative probability ≥ p.\n    \"\"\"\n    probs = torch.softmax(logits, dim=-1)\n    sorted_probs, sorted_idx = torch.sort(probs, descending=True)\n    cumprobs = torch.cumsum(sorted_probs, dim=-1)\n    # Find where cumulative prob first exceeds p\n    cutoff = (cumprobs - sorted_probs >= p).sum().item()  # keep tokens 0..cutoff\n    top_probs = sorted_probs[:cutoff + 1]\n    top_idx   = sorted_idx[:cutoff + 1]\n    # Sample from the nucleus\n    chosen_local = torch.multinomial(top_probs, 1).item()\n    return top_idx[chosen_local].item()"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Training Tricks for LSTM LMs"}]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {"type": "text", "marks": [{"type": "bold"}], "text": "Truncated BPTT:"},
                  {"type": "text", "text": " For long sequences, backpropagation through time is truncated to "
                  },
                  {"type": "text", "marks": [{"type": "italic"}], "text": "k"},
                  {"type": "text", "text": " steps. The hidden state is carried forward across chunks (detached from the graph) to maintain long-range context without unbounded memory."}
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {"type": "text", "marks": [{"type": "bold"}], "text": "Variational dropout:"},
                  {"type": "text", "text": " Apply the same dropout mask at each timestep (not fresh per step). This prevents the model from relying on the randomness of different masks at different steps."}
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {"type": "text", "marks": [{"type": "bold"}], "text": "AR + TAR regularisation:"},
                  {"type": "text", "text": " Activation regularisation penalises large activations; temporal AR penalises large differences between consecutive hidden states — encouraging smooth temporal dynamics."}
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {"type": "text", "marks": [{"type": "bold"}], "text": "Gradient clipping:"},
                  {"type": "text", "text": " Clip gradients by global norm (typically 1.0 or 5.0) to stabilise LSTM training, which is prone to exploding gradients."}
                ]
              }
            ]
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "def train_step_tbptt(\n    model: CharLM,\n    x_chunk: torch.Tensor,   # [B, chunk_size+1] tokens for this chunk\n    hidden,                   # detached hidden state from previous chunk\n    optimizer: torch.optim.Optimizer,\n    clip_norm: float = 1.0,\n) -> tuple:\n    \"\"\"One truncated BPTT step: process a chunk, clip gradients, step.\"\"\"\n    optimizer.zero_grad()\n    loss = lm_loss(model, x_chunk)  # cross-entropy over chunk\n    loss.backward()\n    torch.nn.utils.clip_grad_norm_(model.parameters(), clip_norm)\n    optimizer.step()\n    # Detach hidden state: carry context forward without backprop through it\n    new_hidden = tuple(h.detach() for h in hidden) if isinstance(hidden, tuple) else hidden.detach()\n    return loss.item(), new_hidden"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Mental Model"}]
      },
      {
        "type": "blockquote",
        "content": [
          {
            "type": "paragraph",
            "content": [
              {
                "type": "text",
                "text": "A character LM is a typist with a running mental state: after reading every character, they update their sense of where the document is going, and use that to predict what to type next. N-gram models are typists with amnesia who only remember the last 2–4 characters; LSTM-LMs have a notepad (the cell state) to write down and erase longer-term context. The softmax over 256 characters is a probability distribution over what key to press — and perplexity measures how surprised the typist is on average."
              }
            ]
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Key Takeaways"}]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {"type": "text", "text": "Language modelling maximises log p(x) = Σ log p(xₜ | x<t); perplexity exp(-1/T · Σ log p) measures average token-level uncertainty."}
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {"type": "text", "text": "N-gram models are fast and interpretable but suffer exponential sparsity with longer contexts; smoothing (Laplace, Kneser-Ney) partially mitigates this."}
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {"type": "text", "text": "Char-LMs avoid OOV and handle morphology but require longer sequences; word-LMs use shorter sequences but need large vocabularies and <UNK> handling."}
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {"type": "text", "text": "LSTM-LMs replace fixed n-gram context with a recurrent hidden state; weight tying, variational dropout, and truncated BPTT are standard training ingredients."}
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {"type": "text", "text": "Text generation uses temperature scaling, top-k, or nucleus (top-p) sampling to trade diversity for coherence; greedy decoding (temperature→0) produces repetitive output."}
                ]
              }
            ]
          }
        ]
      }
    ]
  }
}
