{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Energy-Based Models" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "An energy-based model (EBM) assigns a scalar energy E(x) to each configuration x. Lower energy means higher probability — the model says that plausible configurations settle into low-energy states, just as physical systems tend toward configurations of minimum potential energy. The probability distribution is:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "P(\\mathbf{x}) = \\frac{e^{-E(\\mathbf{x})}}{Z}, \\quad Z = \\sum_{\\mathbf{x}'} e^{-E(\\mathbf{x}')}"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Z is the partition function — a normalising constant that sums (or integrates) over all possible configurations. For binary vectors of length d, Z is a sum over 2ᵈ terms. This is the core difficulty: Z is generally intractable, which is why approximate inference (MCMC, variational methods) dominates EBM training."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Boltzmann Distribution" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The form P ∝ exp(−E) is the Boltzmann (or Gibbs) distribution from statistical mechanics, where E is the energy and the temperature T controls the spread. In machine learning, T is usually absorbed into the energy or set to 1:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "P(\\mathbf{x}) \\propto \\exp\\!\\left(-E(\\mathbf{x}) / T\\right)"
        }
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "T→0: probability concentrates on the minimum-energy configuration (argmax)." }] }]
          },
          {
            "type": "listItem",
            "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "T→∞: distribution approaches uniform — all configurations equally likely." }] }]
          },
          {
            "type": "listItem",
            "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "T=1: the standard machine-learning regime; the energy directly sets log-probabilities up to a constant." }] }]
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Boltzmann Machine Energy" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "A Boltzmann machine is a network of binary stochastic units v ∈ {0,1}ⁿ with pairwise interactions. Its energy function is:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "E(\\mathbf{v}) = -\\sum_{i} b_i v_i - \\sum_{i < j} W_{ij} v_i v_j"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "where bᵢ are per-unit biases and Wᵢⱼ are symmetric pairwise connection weights. A positive Wᵢⱼ means units i and j prefer to be in the same state (both on or both off); a negative Wᵢⱼ means they prefer opposite states."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": { "language": "python" },
        "content": [
          {
            "type": "text",
            "text": "import numpy as np\n\ndef boltzmann_energy(v, b, W):\n    \"\"\"\n    v: binary state vector (n,)\n    b: bias vector (n,)\n    W: weight matrix (n, n), symmetric, zero diagonal\n    Returns: scalar energy\n    \"\"\"\n    bias_term      = -b @ v\n    interaction_term = -0.5 * v @ W @ v   # 0.5 avoids double-counting i<j\n    return bias_term + interaction_term\n\n# Simple 3-unit example\nn = 3\nrng = np.random.default_rng(0)\nW = rng.normal(0, 0.5, (n, n))\nW = (W + W.T) / 2   # symmetrize\nnp.fill_diagonal(W, 0)\nb = rng.normal(0, 0.5, n)\n\n# Enumerate all 2^n=8 configurations and compute their energies\nfrom itertools import product as iproduct\nconfigs = np.array(list(iproduct([0, 1], repeat=n)))\nenergies = np.array([boltzmann_energy(v, b, W) for v in configs])\n\n# Convert to probabilities via softmax over -energies\nlog_probs = -energies - np.log(np.exp(-energies).sum())\nprobs = np.exp(log_probs)\nfor cfg, p in zip(configs, probs):\n    print(f\"{cfg} → E={boltzmann_energy(cfg,b,W):.2f}  P={p:.4f}\")"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Why Energy Functions Are Expressive" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The energy function can be any real-valued function — there is no requirement for it to factorise, normalise, or satisfy any conditional independence property. This means energy-based models can represent complex multimodal distributions, including distributions with long-range correlations that directional graphical models (Bayes nets) cannot represent without intractable inference."
          }
        ]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The trade-off is the partition function Z. In a directed model, Z=1 by construction (probabilities sum to 1 along each conditional). In an EBM, Z must be explicitly computed or approximated at training time, which is where methods like contrastive divergence, MCMC, and score matching come in."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "The Log-Likelihood Gradient" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Training an EBM by maximising the log-likelihood of a dataset {x⁽ⁿ⁾} gives a gradient that is the difference of two expectations:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "\\frac{\\partial \\log P(\\mathbf{x})}{\\partial \\theta} = -\\frac{\\partial E(\\mathbf{x})}{\\partial \\theta} + \\mathbb{E}_{P}\\!\\left[\\frac{\\partial E(\\mathbf{x}')}{\\partial \\theta}\\right]"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The first term (the negative data energy gradient) is easy — it just requires evaluating the energy at the observed data point. The second term (the model expectation) requires averaging over samples from the model distribution P — this is the intractable part. The contrastive divergence algorithm approximates this second term with a short MCMC chain started from a data point."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Connection to Modern Models" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Energy-based thinking recurs throughout deep learning. A softmax classifier assigns scores (negative energies) to each class and normalises with a partition function over the class set. Score matching — used in diffusion model training — avoids computing Z by matching the gradient of the log-density (the score ∇x log P(x)) rather than the density itself. Neural network potentials for molecular simulation are EBMs over atom configurations. The energy function perspective unifies these as a single framework: define an energy, define a distribution via the Boltzmann transform, and approximate learning using one of the available partition-function-free estimators."
          }
        ]
      }
    ]
  }
}
