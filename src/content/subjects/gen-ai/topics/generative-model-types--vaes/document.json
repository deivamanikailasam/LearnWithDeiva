{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Variational Autoencoders (VAEs) are latent variable generative models that learn a compressed, continuous representation of data. An encoder maps each input x to a distribution over a latent space z, and a decoder reconstructs x from z. Unlike plain autoencoders, the latent space is explicitly regularised to follow a prior distribution (typically Gaussian), which enables sampling new data by drawing z ~ N(0, I) and decoding. The training objective — the ELBO — makes this tractable without computing the intractable marginal likelihood p(x)."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "The Generative Story"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "A VAE defines a latent variable model with a simple prior over z and a learned conditional over x:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "z \\sim p(z) = \\mathcal{N}(0, I), \\quad x \\sim p_\\theta(x \\mid z)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The marginal p_θ(x) = ∫ p_θ(x | z) p(z) dz is intractable because the integral is over all z ∈ ℝᵈ. The maximum likelihood objective log p_θ(x) cannot be optimised directly. VAEs instead optimise a lower bound — the ELBO — obtained by introducing an approximate posterior q_φ(z | x):"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "\\log p_\\theta(x) \\geq \\underbrace{\\mathbb{E}_{z \\sim q_\\phi(z|x)}[\\log p_\\theta(x \\mid z)]}_{\\text{reconstruction}} - \\underbrace{D_{\\text{KL}}\\bigl(q_\\phi(z|x) \\,\\|\\, p(z)\\bigr)}_{\\text{KL regularisation}}"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The reconstruction term rewards the decoder for recovering x from sampled z. The KL term penalises the approximate posterior for deviating from the prior, regularising the latent space to be smooth and well-structured."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Implementation: Encoder, Reparameterisation, Decoder"}]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "import torch\nimport torch.nn as nn\nfrom torch import Tensor\n\n\nclass VAE(nn.Module):\n    \"\"\"\n    Variational Autoencoder for continuous data.\n    Encoder:  x -> (mu, log_var) parameterising q_phi(z|x) = N(mu, diag(exp(log_var)))\n    Decoder:  z -> x_recon, parameterising p_theta(x|z) = N(x_recon, I) (or Bernoulli)\n    \"\"\"\n    def __init__(self, input_dim: int = 784, hidden_dim: int = 256, latent_dim: int = 32):\n        super().__init__()\n        # Encoder: maps x to parameters of q(z|x)\n        self.encoder = nn.Sequential(\n            nn.Linear(input_dim, hidden_dim), nn.ReLU(),\n            nn.Linear(hidden_dim, hidden_dim), nn.ReLU(),\n        )\n        self.mu_head      = nn.Linear(hidden_dim, latent_dim)\n        self.log_var_head = nn.Linear(hidden_dim, latent_dim)\n\n        # Decoder: maps z to x reconstruction\n        self.decoder = nn.Sequential(\n            nn.Linear(latent_dim, hidden_dim), nn.ReLU(),\n            nn.Linear(hidden_dim, hidden_dim), nn.ReLU(),\n            nn.Linear(hidden_dim, input_dim),\n        )\n\n    def encode(self, x: Tensor) -> tuple[Tensor, Tensor]:\n        \"\"\"Returns (mu, log_var) of the approximate posterior q(z|x).\"\"\"\n        h = self.encoder(x)\n        return self.mu_head(h), self.log_var_head(h)\n\n    def reparameterise(self, mu: Tensor, log_var: Tensor) -> Tensor:\n        \"\"\"\n        Reparameterisation trick: z = mu + eps * std, eps ~ N(0, I).\n        Makes the sampling step differentiable with respect to mu and log_var.\n        Without this trick, we could not backpropagate through the stochastic node.\n        \"\"\"\n        std = (0.5 * log_var).exp()      # std = exp(log_var / 2)\n        eps = torch.randn_like(std)      # eps ~ N(0, I)\n        return mu + eps * std            # z ~ N(mu, std^2)\n\n    def decode(self, z: Tensor) -> Tensor:\n        \"\"\"Returns reconstructed x from latent z.\"\"\"\n        return self.decoder(z)           # (B, input_dim) logits or values\n\n    def forward(self, x: Tensor) -> tuple[Tensor, Tensor, Tensor]:\n        \"\"\"Returns (x_recon, mu, log_var).\"\"\"\n        mu, log_var = self.encode(x)\n        z = self.reparameterise(mu, log_var)\n        return self.decode(z), mu, log_var\n\n\ndef elbo_loss(\n    x: Tensor,\n    x_recon: Tensor,\n    mu: Tensor,\n    log_var: Tensor,\n    beta: float = 1.0,\n) -> tuple[Tensor, dict]:\n    \"\"\"\n    ELBO loss = reconstruction loss + beta * KL divergence.\n\n    Reconstruction: MSE for continuous data, BCE for binary data.\n    KL: closed form for Gaussian prior p(z) = N(0,I) and Gaussian posterior q(z|x):\n        KL(N(mu, var) || N(0,I)) = 0.5 * sum(1 + log_var - mu^2 - exp(log_var))\n    \"\"\"\n    # Reconstruction loss (Gaussian decoder -> MSE)\n    recon_loss = nn.functional.mse_loss(x_recon, x, reduction='sum') / x.shape[0]\n\n    # KL divergence (closed form)\n    kl_loss = -0.5 * (1 + log_var - mu.pow(2) - log_var.exp()).sum(dim=-1).mean()\n\n    total = recon_loss + beta * kl_loss\n    return total, {\"recon\": recon_loss.item(), \"kl\": kl_loss.item()}\n\n\n@torch.no_grad()\n def sample(model: VAE, n: int, latent_dim: int, device) -> Tensor:\n    \"\"\"Generate new samples by drawing z ~ N(0,I) and decoding.\"\"\"\n    z = torch.randn(n, latent_dim, device=device)\n    return model.decode(z)\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "The Reparameterisation Trick"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The ELBO involves an expectation over z ~ q_φ(z | x). Backpropagating through a sampling operation is not possible directly — the gradient cannot flow through a random node. The reparameterisation trick resolves this by writing z as a deterministic function of the parameters plus independent noise:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "z = \\mu_\\phi(x) + \\sigma_\\phi(x) \\odot \\varepsilon, \\quad \\varepsilon \\sim \\mathcal{N}(0, I)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Now z is a deterministic function of φ (the encoder parameters) with ε as external noise. The gradient ∂z/∂φ exists and can be computed by automatic differentiation, enabling end-to-end training of both the encoder and decoder."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "The KL Term as Regularisation"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The KL divergence term KL(q_φ(z|x) ‖ p(z)) has a closed form for Gaussian posteriors and a Gaussian prior:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "D_{\\text{KL}}\\bigl(\\mathcal{N}(\\mu, \\sigma^2) \\,\\|\\, \\mathcal{N}(0, I)\\bigr) = \\frac{1}{2} \\sum_{j=1}^{d}\\left(1 + \\log\\sigma_j^2 - \\mu_j^2 - \\sigma_j^2\\right)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "This term pushes the posterior toward the prior: μ → 0 and σ → 1. Without it, the encoder collapses to a point estimate (σ → 0) and the model degenerates to a deterministic autoencoder with no structured latent space. The KL term is what makes the latent space continuous and interpolatable."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Latent Space Properties"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The VAE latent space has useful geometric properties that a plain autoencoder lacks:"
          }
        ]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Continuity: nearby points in z decode to similar x. Linear interpolation between two latent codes produces a smooth transition in data space."}]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Completeness: the entire prior support (all of N(0,I)) maps to plausible x. Sampling z ~ N(0,I) always produces a valid output, not just the training points."}]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Disentanglement (β-VAE): increasing β forces the KL term to dominate, pushing individual latent dimensions to encode independent factors of variation."}]}]
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "@torch.no_grad()\ndef interpolate_latents(\n    model: VAE,\n    x1: Tensor,\n    x2: Tensor,\n    n_steps: int = 10,\n) -> list[Tensor]:\n    \"\"\"\n    Linearly interpolate between two latent codes and decode each step.\n    With a well-trained VAE, intermediate points decode to semantically\n    meaningful intermediate states (e.g. face morphing between two identities).\n    \"\"\"\n    mu1, _ = model.encode(x1)\n    mu2, _ = model.encode(x2)\n    frames = []\n    for alpha in torch.linspace(0, 1, n_steps):\n        z = (1 - alpha) * mu1 + alpha * mu2\n        frames.append(model.decode(z))\n    return frames\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "VAE Limitations"}]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Blurry outputs: the Gaussian decoder minimises MSE, which averages over all plausible reconstructions. The result is often blurry for images. Perceptual losses or adversarial training can partially mitigate this."}]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Posterior collapse: in sequence VAEs (text), the decoder can become powerful enough to ignore z entirely, causing the KL term to collapse to zero (the encoder learns to output the prior). Mitigations include KL annealing, free bits, and weakening the decoder."}]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "ELBO gap: the gap between the ELBO and the true log-likelihood depends on how well q_φ(z|x) approximates the true posterior p_θ(z|x). A unimodal Gaussian q is too restrictive for multimodal true posteriors."}]}]
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "VAEs as Foundation Model Components"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "VAEs underpin latent diffusion models (Stable Diffusion, DALL-E 3). A VAE encoder compresses a 512×512 image (786,432 pixels) into an 8-channel 64×64 latent (32,768 values) — a 24× compression. The diffusion process then operates entirely in this compact latent space, reducing compute by ~576× compared to pixel-space diffusion. The VAE decoder reconstructs the final image from the denoised latent. In this role the VAE is pre-trained and frozen; only the diffusion model is trained."
          }
        ]
      },
      {
        "type": "blockquote",
        "content": [
          {
            "type": "paragraph",
            "content": [
              {
                "type": "text",
                "text": "Mental model: a VAE is an information bottleneck with a probabilistic valve. The encoder squeezes each input into a tiny cloud of uncertainty in latent space (a Gaussian, not a point). The KL term keeps all clouds near the origin so the whole space is filled and navigable. The decoder reconstructs x from any point in that space. The key insight is that constraining the latent to be a cloud (not a point) is what makes the model generative — by sampling from the prior N(0,I) you always land inside some cloud and can decode a plausible output."
              }
            ]
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Key Takeaways"}]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "VAEs combine a probabilistic encoder q_φ(z|x) and decoder p_θ(x|z); training maximises the ELBO = reconstruction − KL, a tractable lower bound on log p_θ(x)."}]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "The reparameterisation trick (z = μ + σ⊙ε) makes the stochastic sampling step differentiable, enabling end-to-end gradient-based training."}]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "The KL term regularises the latent space to match N(0,I), ensuring continuity and completeness — properties absent in plain autoencoders."}]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Main limitations: blurry image outputs from MSE reconstruction, and posterior collapse in powerful decoders. β-VAE improves disentanglement at the cost of reconstruction quality."}]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Latent diffusion models (Stable Diffusion) use a pre-trained VAE to compress images into a compact latent space, enabling diffusion training and sampling at a fraction of pixel-space cost."}]}]
          }
        ]
      }
    ]
  }
}
