{
  "definition": "The chain rule states that the derivative of a composite function f(g(x)) equals the outer derivative evaluated at the inner output, times the inner derivative: (f∘g)′(x) = f′(g(x))·g′(x).",
  "layman": "When one calculation feeds into another, the overall sensitivity to the original input is the product of how sensitive each step is along the path—like multiplying gear ratios in a sequence.",
  "content": "## One-variable chain rule\n\nIf **y = f(u)** and **u = g(x)**, then by composition **y = f(g(x))**. The **chain rule** is\n\n**dy/dx = (dy/du) · (du/dx)**\n\nEquivalently: **(f∘g)′(x) = f′(g(x)) · g′(x)**.\n\n### Intuition\n\nA tiny change Δx perturbs u by about g′(x)·Δx; that perturbs y by about f′(u)·Δu. Multiplying gives the overall rate **f′(g(x))·g′(x)**.\n\n### Worked example\n\nFor **h(x) = sin(x²)**:\n- Outer f(u) = sin u, inner g(x) = x²\n- f′(u) = cos u, g′(x) = 2x\n- h′(x) = cos(x²) · 2x\n\n### Multivariable form (preview)\n\nWhen several paths feed one output, contributions **add**:\n\n**∂z/∂t = (∂z/∂x)(∂x/∂t) + (∂z/∂y)(∂y/∂t)**\n\nThis generalizes to gradients and Jacobians in higher dimensions.\n\n### Why the chain rule is the engine of backpropagation\n\nA neural network is a **nested composition** of layers:\n\n**x → z¹ = W¹x + b¹ → a¹ = σ(z¹) → z² = W²a¹ + b² → … → L(a^L)**\n\nTraining needs **∂L/∂W^ℓ** for every layer ℓ. Backpropagation applies the chain rule **from the loss backward**:\n\n1. Compute forward pass values (activations, pre-activations).\n2. At the loss, start with **∂L/∂a^L**.\n3. For each layer, multiply by local Jacobians/derivatives and pass the signal to earlier layers.\n4. Accumulate **∂L/∂W^ℓ** using the local rule (e.g. outer product of upstream gradient and layer input).\n\nSo the chain rule is not a side topic in ML—it is **how gradients flow through composed functions** that frameworks implement as `loss.backward()`.\n\n### Automatic differentiation\n\nModern autograd records the computation graph and applies the chain rule symbolically on primitives (add, mul, matmul, σ, …). You write the forward pass; the library chains local derivatives automatically—exactly the chain rule at scale.",
  "keyPoints": [
    "(f∘g)′(x) = f′(g(x))·g′(x): multiply outer and inner local rates of change.",
    "Multivariable chain rule sums contributions along each input path.",
    "Neural networks are deep compositions; backpropagation is reverse-mode chain rule.",
    "Autograd applies the chain rule on a computation graph during loss.backward()."
  ]
}
