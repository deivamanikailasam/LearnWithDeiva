{
  "definition": "**`pyproject.toml`** is the standardised, declarative configuration file for a Python project. It describes (1) **how to build** the project into a distributable package, (2) **what metadata** the package has (name, version, authors, classifiers, dependencies), and (3) **how supporting tools** (linters, formatters, type-checkers, test runners) are configured — all in one file, using TOML.",
  "intuition": "Think of `pyproject.toml` as the project's *passport*. One document that says: this is who I am (`[project]`), this is how to build me (`[build-system]`), and here's how my entourage of tools should treat me (`[tool.*]`). Before `pyproject.toml`, you had `setup.py` (executable code!), `setup.cfg` (different syntax), `MANIFEST.in`, plus separate dotfiles for every tool. Now it's one file, declarative, standardised by PEPs, understood by every modern build backend.",
  "explanation": "### The three roles of pyproject.toml\n\n#### 1. `[build-system]` — how to build the project\n\n```toml\n[build-system]\nrequires = [\"setuptools>=64\", \"wheel\"]\nbuild-backend = \"setuptools.build_meta\"\n```\n\nThis is the contract with `pip`, `build`, and other front-ends (PEP 517 / PEP 518): *to build a wheel from this source tree, install these packages and call this backend module*. The build backend is pluggable — common choices: **setuptools**, **hatchling**, **flit-core**, **poetry-core**, **pdm-backend**. Each has the same `[build-system]` declaration shape, just with different `requires` and `build-backend` values.\n\nThe key insight: `pip install .` doesn't run *your* code to build the package. It reads `[build-system]`, installs the backend in an isolated env, and asks the backend to produce a wheel. No `setup.py` execution, no surprises.\n\n#### 2. `[project]` — metadata (PEP 621)\n\n```toml\n[project]\nname = \"ragapp\"\nversion = \"0.1.0\"\ndescription = \"A small retrieval-augmented generation toolkit.\"\nreadme = \"README.md\"\nrequires-python = \">=3.10\"\nlicense = { text = \"MIT\" }\nauthors = [{ name = \"Deiva\", email = \"deiva@example.com\" }]\ndependencies = [\n  \"numpy>=1.24\",\n  \"httpx>=0.27\",\n]\n\n[project.optional-dependencies]\ndev = [\"pytest>=8\", \"mypy>=1.10\", \"ruff>=0.5\"]\n\n[project.scripts]\nragapp = \"ragapp.cli:main\"\n```\n\nThis section is backend-agnostic — it works the same with setuptools, hatch, flit, etc. It declares:\n\n- **Identity:** `name`, `version`, `description`, `readme`, `license`, `authors`.\n- **Compatibility:** `requires-python`, `classifiers`.\n- **Dependencies:** `dependencies` for required deps; `[project.optional-dependencies]` for extras (`pip install ragapp[dev]`).\n- **Entry points:** `[project.scripts]` for CLI commands; `[project.entry-points.\"some.group\"]` for plugin systems.\n\nNote `version` can be **static** (as above) or **dynamic** (`dynamic = ['version']` + a backend-specific way to read it from `__init__.py` or git).\n\n#### 3. `[tool.<name>]` — tool configuration\n\n```toml\n[tool.setuptools.packages.find]\nwhere = [\"src\"]\n\n[tool.pytest.ini_options]\ntestpaths = [\"tests\"]\naddopts = \"-q --strict-markers\"\n\n[tool.ruff]\nline-length = 100\ntarget-version = \"py310\"\n\n[tool.mypy]\nstrict = true\n```\n\nEvery tool that supports `pyproject.toml` reads from its own `[tool.<name>]` table. This is *the* place to put tool config — not `setup.cfg`, not `.flake8`, not `mypy.ini` — keeping all project configuration in one file.\n\n### The build-and-distribution flow\n\n```\nsource tree            pip / build               PyPI\n┌─────────────┐      ┌──────────────┐         ┌─────────┐\n│ pyproject + │ ───► │ build backend │ ───►   │ wheel + │\n│ src/ragapp/ │      │  (setuptools, │ ┌───►  │  sdist  │\n└─────────────┘      │   hatch, ...) │ │      └─────────┘\n                     └──────────────┘ │\n                                       │ twine upload\n                                       ▼\n                                  end users\n                                  pip install ragapp\n```\n\nYou never write the wheel by hand. `python -m build` produces both an sdist (source distribution) and a wheel from `pyproject.toml`. `twine upload dist/*` ships them.\n\n### Dependencies vs lock files\n\n`pyproject.toml`'s `dependencies` declares **abstract** requirements (`numpy>=1.24`). For **reproducible installs** (exact versions of every transitive dep), you need a lock file: `requirements.txt` from `pip-tools`, `poetry.lock`, `pdm.lock`, or `uv.lock`. Lock files are *not* the same artefact as `pyproject.toml` — they complement it.\n\n### Build backends compared (briefly)\n\n- **setuptools**: most ubiquitous; powerful but the legacy is showing; great for existing projects.\n- **hatchling**: modern, clean, plugin-friendly; default for `hatch`.\n- **flit-core**: minimal; great for pure-Python single-package projects.\n- **poetry-core / pdm-backend**: tied to their respective workflow tools; bring locking.\n\nThe `[project]` and `[tool.*]` parts are identical across backends; only `[build-system]` and a few backend-specific bits (e.g. `[tool.setuptools.packages.find]` vs `[tool.hatch.build.targets.wheel]`) differ.\n\n### Why this matters\n\nBefore `pyproject.toml`: every project was a little snowflake — different `setup.py` tricks, different config locations, different ways to declare dependencies. After: one file, one set of standards (PEP 517, 518, 621, 660), one place for all configuration. Tools cooperate instead of conflict."
}
