{
  "items": [
    {
      "title": "Adopt `src/` for any project beyond a single file",
      "avoid": "Flat layout for anything that has tests, CI, or will be installed by users — the ambient cwd-based imports hide packaging bugs.",
      "prefer": "Scaffold `src/<pkg>/__init__.py`, `pyproject.toml`, and `tests/` from day one; install with `pip install -e .`.",
      "why": "The cost is one extra install step; the payoff is layout-induced correctness — tests and CI run against the installed package, exactly like users.",
      "example": "```\nmypkg/\n├── pyproject.toml\n├── src/\n│   └── mypkg/__init__.py\n└── tests/test_smoke.py\n```"
    },
    {
      "title": "Pair `src/` with editable installs in CI",
      "avoid": "Running tests in CI from the source tree without installing the package first.",
      "prefer": "First CI step: `pip install -e .[dev]`; then `pytest`, `mypy`, `ruff`.",
      "why": "CI then tests exactly what users will install — same import path, same metadata, same surfaces.",
      "example": "```yaml\nsteps:\n  - run: pip install -e \".[dev]\"\n  - run: pytest -q\n```"
    },
    {
      "title": "Don't add `src/` to `sys.path` manually",
      "avoid": "`sys.path.insert(0, 'src')` in `conftest.py` to 'make imports work without installing'.",
      "prefer": "Run `pip install -e .` once; let Python find the package the normal way.",
      "why": "Manual path manipulation re-creates the flat-layout trap — ambient imports work locally but break for any user who actually installs the package.",
      "example": "If you're tempted to write `sys.path.insert(0, 'src')`, fix the install instead."
    },
    {
      "title": "Keep tests in `tests/` at the repo root (a sibling of `src/`)",
      "avoid": "Placing tests inside `src/<pkg>/tests/` where they could be shipped in the wheel.",
      "prefer": "Top-level `tests/` directory; tests import the package as a user would (`from mypkg.x import y`).",
      "why": "Tests are not part of what ships. Keeping them outside `src/` prevents accidental shipping and keeps the import direction one-way.",
      "example": "```\nsrc/mypkg/...   # ships\ntests/test_x.py # does not ship\n```"
    },
    {
      "title": "Verify wheel contents before publishing",
      "avoid": "Uploading to PyPI without inspecting the artefact — extra files or wrong layout slip through silently.",
      "prefer": "`python -m build && unzip -l dist/*.whl && twine check dist/*` as a pre-publish ritual.",
      "why": "The wheel is the user-facing artefact; confirming it once locally avoids yanked releases later.",
      "example": "```bash\npython -m build\nunzip -l dist/mypkg-0.1.0-py3-none-any.whl | grep mypkg\ntwine check dist/*\n```"
    }
  ]
}
