{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Byte Pair Encoding (BPE) is the tokenisation algorithm used by GPT-2, GPT-3, GPT-4, LLaMA, Mistral, and most major decoder-only language models. It starts with individual bytes or characters and iteratively merges the most frequent adjacent pair into a new token, until the vocabulary reaches the target size. The result is a vocabulary that contains individual characters for rare inputs and common multi-character subwords (\"ing\", \"tion\", \"##al\") or even whole words for frequent ones. BPE is a compression algorithm repurposed as a tokeniser: it minimises the number of tokens needed to represent the training corpus, which directly maps to model efficiency."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "The BPE Training Algorithm"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Training BPE produces a merge table — an ordered list of pair merges. Applying the merges in order to any new text gives the tokenisation. The algorithm:"
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "from collections import Counter\n\n\ndef train_bpe(corpus: list[str], vocab_size: int) -> list[tuple[str, str]]:\n    \"\"\"\n    Train a BPE tokeniser on a list of strings.\n    Returns the ordered merge list.\n\n    This is a simplified version that works on characters.\n    Production BPE (tiktoken, HuggingFace) operates on UTF-8 bytes.\n    \"\"\"\n    # Initialise: split each word into characters + end-of-word marker.\n    # The space prefix (Ġ) is GPT-style; BERT uses ## suffix instead.\n    words = Counter()\n    for text in corpus:\n        for word in text.split():\n            words[(\" \".join(list(word)) + \" </w>\",)] += 1\n\n    # Current vocabulary: all individual characters present\n    vocab = set(ch for word_tuple in words for word in word_tuple for ch in word.split())\n    merges: list[tuple[str, str]] = []\n\n    while len(vocab) < vocab_size:\n        # Count all adjacent pairs across the corpus\n        pair_counts: Counter = Counter()\n        for (word,), freq in words.items():\n            symbols = word.split()\n            for i in range(len(symbols) - 1):\n                pair_counts[(symbols[i], symbols[i + 1])] += freq\n\n        if not pair_counts:\n            break\n\n        # Pick the most frequent pair\n        best_pair = max(pair_counts, key=pair_counts.__getitem__)\n        merges.append(best_pair)\n        new_token = \"\".join(best_pair)\n        vocab.add(new_token)\n\n        # Merge that pair everywhere in the corpus\n        new_words: Counter = Counter()\n        merge_str = \" \".join(best_pair)\n        replacement = new_token\n        for (word,), freq in words.items():\n            new_word = word.replace(merge_str, replacement)\n            new_words[(new_word,)] += freq\n        words = new_words\n\n    return merges\n\n\n# Example\ncorpus = [\n    \"low low low low\",\n    \"lower lower\",\n    \"newest newest newest\",\n    \"widest widest\",\n]\nmerges = train_bpe(corpus, vocab_size=20)\nprint(\"Merge list:\")\nfor pair in merges:\n    print(f\"  {pair[0]!r} + {pair[1]!r} → {pair[0]+pair[1]!r}\")\n# e.g.:\n# 'l' + 'o' → 'lo'\n# 'lo' + 'w' → 'low'\n# 'e' + 's' → 'es'\n# 'es' + 't' → 'est'\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Encoding New Text with a Learned Merge Table"}]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "def apply_bpe(text: str, merges: list[tuple[str, str]]) -> list[str]:\n    \"\"\"\n    Encode a string using a trained BPE merge table.\n    Returns the list of subword tokens.\n    \"\"\"\n    # Start with character-level split\n    tokens = list(text) + [\"</w>\"]\n\n    # Apply each merge in the order it was learned\n    for pair in merges:\n        merged = \"\".join(pair)\n        i = 0\n        new_tokens = []\n        while i < len(tokens):\n            if i < len(tokens) - 1 and (tokens[i], tokens[i + 1]) == pair:\n                new_tokens.append(merged)\n                i += 2\n            else:\n                new_tokens.append(tokens[i])\n                i += 1\n        tokens = new_tokens\n\n    return tokens\n\n\n# Using HuggingFace tokenisers in practice (wraps the same algorithm)\nfrom transformers import AutoTokenizer\n\ntokenizer = AutoTokenizer.from_pretrained(\"gpt2\")\n\nexamples = [\n    \"Hello, world!\",\n    \"tokenisation\",\n    \"supercalifragilistic\",\n    \"print('hello')\",\n]\nfor text in examples:\n    tokens = tokenizer.tokenize(text)\n    ids    = tokenizer.encode(text)\n    print(f\"{text!r:30s} → {tokens}  ({len(tokens)} tokens)\")\n\n# 'Hello, world!'                → ['Hello', ',', 'Ġworld', '!']  (4 tokens)\n# 'tokenisation'                 → ['token', 'isation']            (2 tokens)\n# 'supercalifragilistic'         → ['super', 'cal', 'if', 'rag', 'il', 'istic'] (6)\n# \"print('hello')\"               → ['print', \"('\", 'hello', \"')\"] (4 tokens)\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Byte-Level BPE: GPT-2's Key Innovation"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Standard BPE operates on Unicode characters, which means rare characters or scripts may not appear in the vocabulary at all, making them unrepresentable. GPT-2 introduced byte-level BPE: start from the 256 possible bytes, so every UTF-8 string is always representable. The merge table then builds up subword units from bytes. This is why GPT-2/3/4 and LLaMA have no unknown-token ([UNK]) issue — every possible byte sequence can be tokenised."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "# tiktoken is OpenAI's fast byte-level BPE implementation\n# pip install tiktoken\nimport tiktoken\n\n# cl100k_base: used by GPT-4 and Claude-class models (100k vocab)\nenc = tiktoken.get_encoding(\"cl100k_base\")\n\ntexts = [\n    \"Hello, world!\",\n    \"日本語テキスト\",          # Japanese — no UNK, handled by byte fallback\n    \"def fibonacci(n):\",\n    \"\\u0000\\u0001\\u0002\",     # raw control bytes — still encodable\n]\nfor text in texts:\n    ids = enc.encode(text)\n    decoded_tokens = [enc.decode([i]) for i in ids]\n    print(f\"{text!r:35s} → {decoded_tokens}  ({len(ids)} tokens)\")\n\n# Key properties of cl100k_base:\n# - 100,277 token vocabulary\n# - Byte fallback: any input is encodable (no UNK)\n# - Special tokens: <|endoftext|>, <|fim_prefix|>, etc.\n# - Numbers are split: '1234' → ['12', '34'] (varies by context)\nprint(f\"Vocab size: {enc.n_vocab}\")\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "BPE Properties and Practical Implications"}]
      },
      {
        "type": "table",
        "content": [
          {
            "type": "tableRow",
            "content": [
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Property"}]}]},
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Implication for users"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Tokens are not words"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "\"tokenization\" may be 1–3 tokens depending on frequency; count tokens, not characters, when estimating cost"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Numbers are split inconsistently"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "\"1234\" may tokenise as [\"12\", \"34\"] — arithmetic over tokens is unreliable without chain-of-thought"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Whitespace is part of tokens"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "\"dog\" and \" dog\" are different tokens; inserting a space before a word changes the tokenisation"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Code is tokenised efficiently"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Common keywords (\"def\", \"return\", \"import\") are single tokens; code-heavy training data biases toward this"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Non-Latin scripts use more tokens"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Chinese/Japanese characters often tokenise to 1–3 tokens each vs. ~0.75 tokens per English word — same content costs more in non-English"}]}]}
            ]
          }
        ]
      },
      {
        "type": "blockquote",
        "content": [
          {
            "type": "paragraph",
            "content": [
              {
                "type": "text",
                "text": "BPE is an elegant compression algorithm that happens to produce good tokenisations for language models: common words become single tokens (short sequences to predict), rare words decompose into meaningful subwords (the model can reason about morphology), and the byte fallback guarantees no text is ever unrepresentable. The merge table is the entire tokeniser — it is deterministic, fast to apply, and trivially serialisable."
              }
            ]
          }
        ]
      }
    ]
  }
}
