{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Pretraining is the first and most expensive phase in the modern LLM development pipeline. A model is trained on a massive unlabelled corpus — typically hundreds of billions to trillions of tokens of text — using a self-supervised objective that requires no human annotation. The model learns to predict masked or future tokens, and in doing so develops representations of grammar, facts, reasoning patterns, and world knowledge that transfer to virtually any downstream NLP task. Pretraining produces a general-purpose base model; it does not produce a useful assistant. That requires a second phase (fine-tuning or instruction tuning)."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "The Two Dominant Pretraining Objectives"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "All major pretraining objectives are variants of one idea: hide part of the input and train the model to recover it."
          }
        ]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [
              {"type": "text", "marks": [{"type": "bold"}], "text": "Causal (autoregressive) LM — used by GPT family, LLaMA, Mistral. "},
              {"type": "text", "text": "Predict the next token given all previous tokens. The model sees tokens left-to-right only; a causal mask prevents attending to future positions. Natural for generation."}
            ]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [
              {"type": "text", "marks": [{"type": "bold"}], "text": "Masked LM (MLM) — used by BERT, RoBERTa, DeBERTa. "},
              {"type": "text", "text": "Replace ~15% of tokens with a [MASK] token and predict their identity. The model attends bidirectionally. Better for representation / classification tasks; cannot generate text autoregressively."}
            ]}]
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "\\mathcal{L}_{\\text{CLM}} = -\\sum_{t=1}^{T} \\log P_\\theta(x_t \\mid x_{<t}), \\qquad \\mathcal{L}_{\\text{MLM}} = -\\sum_{i \\in \\mathcal{M}} \\log P_\\theta(x_i \\mid x_{\\setminus \\mathcal{M}})"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "where M is the set of masked positions. Modern generative models all use causal LM; BERT-style MLM fell out of favour for generative applications after GPT-3 demonstrated that causal LM at scale produces equally strong representations."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Implementing a Minimal Causal LM Pretraining Loop"}]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "import torch\nimport torch.nn as nn\nfrom torch.utils.data import DataLoader, IterableDataset\nfrom transformers import AutoTokenizer, AutoConfig, AutoModelForCausalLM\nfrom transformers import get_cosine_schedule_with_warmup\n\n\nclass StreamingTextDataset(IterableDataset):\n    \"\"\"\n    Minimal streaming dataset that tokenises text on the fly and packs\n    tokens into fixed-length chunks (no padding waste).\n    \"\"\"\n    def __init__(self, texts: list[str], tokenizer, seq_len: int = 2048):\n        self.tokenizer = tokenizer\n        self.seq_len   = seq_len\n        self.texts     = texts\n\n    def __iter__(self):\n        buffer = []\n        for text in self.texts:\n            tokens = self.tokenizer.encode(text, add_special_tokens=False)\n            buffer.extend(tokens)\n            # Yield as many complete chunks as possible\n            while len(buffer) >= self.seq_len + 1:\n                chunk = buffer[:self.seq_len + 1]\n                buffer = buffer[self.seq_len + 1:]\n                input_ids = torch.tensor(chunk[:-1], dtype=torch.long)\n                labels    = torch.tensor(chunk[1:],  dtype=torch.long)  # shift by 1\n                yield {\"input_ids\": input_ids, \"labels\": labels}\n\n\ndef pretrain(\n    model_name_or_config: str,\n    texts: list[str],\n    num_steps: int = 1000,\n    batch_size: int = 4,\n    lr: float = 3e-4,\n    warmup_steps: int = 100,\n    seq_len: int = 2048,\n):\n    \"\"\"\n    Minimal causal LM pretraining loop.\n    In practice you would use FSDP/DeepSpeed, gradient checkpointing,\n    and a distributed data loader — this illustrates the core logic.\n    \"\"\"\n    tokenizer = AutoTokenizer.from_pretrained(\"gpt2\")  # reuse tokenizer for example\n    tokenizer.pad_token = tokenizer.eos_token\n\n    # For a real run: AutoModelForCausalLM.from_config(AutoConfig.from_pretrained(...))\n    model = AutoModelForCausalLM.from_pretrained(model_name_or_config)\n    model = model.cuda().to(torch.bfloat16)\n\n    dataset = StreamingTextDataset(texts, tokenizer, seq_len=seq_len)\n    loader  = DataLoader(dataset, batch_size=batch_size)\n\n    optimizer = torch.optim.AdamW(\n        model.parameters(),\n        lr=lr,\n        betas=(0.9, 0.95),   # standard for LLM pretraining\n        weight_decay=0.1,\n        eps=1e-8,\n    )\n    scheduler = get_cosine_schedule_with_warmup(\n        optimizer, num_warmup_steps=warmup_steps, num_training_steps=num_steps\n    )\n\n    model.train()\n    for step, batch in enumerate(loader):\n        if step >= num_steps:\n            break\n\n        input_ids = batch[\"input_ids\"].cuda()\n        labels    = batch[\"labels\"].cuda()\n\n        with torch.autocast(device_type=\"cuda\", dtype=torch.bfloat16):\n            outputs = model(input_ids=input_ids, labels=labels)\n            loss    = outputs.loss\n\n        loss.backward()\n        # Gradient clipping: prevents loss spikes common in early pretraining\n        torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\n        optimizer.step()\n        scheduler.step()\n        optimizer.zero_grad(set_to_none=True)\n\n        if step % 100 == 0:\n            import math\n            print(f\"step {step:5d} | loss {loss.item():.4f} | ppl {math.exp(loss.item()):.1f}\")\n\n    return model\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Tokenisation: The Bridge Between Text and Tensors"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Before training, text must be converted to integer token IDs. Modern LLMs use Byte Pair Encoding (BPE) or its variants (SentencePiece, Unigram LM). BPE starts with byte-level tokens and iteratively merges the most frequent adjacent pair, building a vocabulary of ~32 000–128 000 subword units. The vocabulary is fixed before pretraining and shared between training and inference."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "from tokenizers import Tokenizer, models, trainers, pre_tokenizers\n\n\ndef train_bpe_tokenizer(\n    texts: list[str],\n    vocab_size: int = 32_000,\n    special_tokens: list[str] | None = None,\n) -> Tokenizer:\n    \"\"\"\n    Train a byte-level BPE tokeniser on a corpus of texts.\n    In practice, train on the full pretraining corpus (or a representative sample).\n    \"\"\"\n    if special_tokens is None:\n        special_tokens = [\"<|endoftext|>\", \"<|pad|>\"]\n\n    tokenizer = Tokenizer(models.BPE())\n    tokenizer.pre_tokenizer = pre_tokenizers.ByteLevel(add_prefix_space=False)\n\n    trainer = trainers.BpeTrainer(\n        vocab_size=vocab_size,\n        special_tokens=special_tokens,\n        min_frequency=2,\n    )\n    tokenizer.train_from_iterator(texts, trainer=trainer)\n    return tokenizer\n\n\n# Once trained, the tokeniser is saved and reused at every subsequent step:\n# tokenizer.save(\"tokenizer.json\")\n# tok = Tokenizer.from_file(\"tokenizer.json\")\n# ids = tok.encode(\"Hello, world!\").ids   # -> [15496, 11, 995, 0]\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "What a Pretrained Model Can and Cannot Do"}]
      },
      {
        "type": "table",
        "content": [
          {
            "type": "tableRow",
            "content": [
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Can do (out of the box)"}]}]},
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Cannot do reliably"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Complete any text prefix plausibly"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Follow an instruction ('summarise this')"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Few-shot in-context learning from examples in the prompt"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Refuse harmful requests"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Encode domain knowledge from pretraining data"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Maintain a consistent persona or tone"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Generate syntactically and grammatically correct text"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Ask clarifying questions instead of making assumptions"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Translate, summarise, or classify when prompted as few-shot"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Distinguish what it knows from what it is guessing"}]}]}
            ]
          }
        ]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "This gap between a pretrained base model and a deployed assistant is why fine-tuning, instruction tuning, and RLHF exist. Pretraining provides the knowledge substrate; the subsequent stages shape how that knowledge is expressed."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Training Dynamics and Loss Curves"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "A healthy pretraining run shows a smoothly decreasing loss with no large spikes. Common failure modes and their causes:"
          }
        ]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [
              {"type": "text", "marks": [{"type": "bold"}], "text": "Loss spikes then recovers. "},
              {"type": "text", "text": "Usually caused by a corrupted or anomalously long batch (outlier document). Gradient clipping (max_norm=1.0) bounds the damage. Some teams use loss-aware batch filtering."}
            ]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [
              {"type": "text", "marks": [{"type": "bold"}], "text": "Loss plateaus early. "},
              {"type": "text", "text": "Learning rate too low, or the warmup period is too long relative to the total run. Check that the scheduler is decaying correctly."}
            ]}]
          },
          {
            "type": "listItem",
            "content": [{"type": "paragraph", "content": [
              {"type": "text", "marks": [{"type": "bold"}], "text": "Loss diverges (NaN). "},
              {"type": "text", "text": "Learning rate too high, or bfloat16 overflow. Reduce lr, check for degenerate inputs (zero-length sequences), and verify gradient scaling."}
            ]}]
          }
        ]
      },
      {
        "type": "blockquote",
        "content": [
          {
            "type": "paragraph",
            "content": [
              {
                "type": "text",
                "text": "Pretraining is irreversible in practice: re-running a multi-thousand GPU-hour job to fix a data quality issue is prohibitively expensive. Invest heavily in data curation, tokenisation verification, and loss monitoring before committing to a full run."
              }
            ]
          }
        ]
      }
    ]
  }
}
