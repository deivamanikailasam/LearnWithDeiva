{
  "criteria": [
    {
      "label": "Make GET/POST requests and read JSON responses with await.",
      "level": "beginner",
      "example": "```ts\nconst r = await fetch('/api/users')\nconst users = await r.json()\n\nconst r2 = await fetch('/api/chat', {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({ message: 'hi' })\n})\n```"
    },
    {
      "label": "Always check res.ok and handle HTTP errors (e.g. 429).",
      "level": "beginner",
      "example": "```ts\nconst r = await fetch(url)\nif (!r.ok) {\n  if (r.status === 429) { await sleep(retryAfter(r)); return retry() }\n  throw new Error(`HTTP ${r.status}: ${await r.text()}`)\n}\nreturn r.json()\n```"
    },
    {
      "label": "Send authenticated JSON POSTs and keep keys server-side.",
      "level": "intermediate",
      "example": "```ts\n// server-only — never in browser code\nawait fetch('https://api.openai.com/v1/chat/completions', {\n  method: 'POST',\n  headers: {\n    'Content-Type': 'application/json',\n    'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`,\n  },\n  body: JSON.stringify(payload),\n})\n```"
    },
    {
      "label": "Add AbortController timeouts and read streamed response bodies.",
      "level": "advanced",
      "example": "```ts\nconst c = new AbortController()\nconst t = setTimeout(() => c.abort(), 5000)\nconst r = await fetch(url, { signal: c.signal })\nclearTimeout(t)\nfor await (const chunk of r.body!) console.write(new TextDecoder().decode(chunk))\n```"
    }
  ]
}
