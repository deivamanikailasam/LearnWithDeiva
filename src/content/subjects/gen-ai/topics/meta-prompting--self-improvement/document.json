{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Self-improving prompts use an LLM to critique and rewrite its own prompts based on observed output quality. The loop is: generate output → evaluate it → identify prompt weaknesses → rewrite the prompt → repeat. Each cycle tightens the instruction until the output meets a quality criterion or a maximum iteration count is reached. This is meta-prompting applied iteratively: the model is both the worker and the prompt engineer."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "The Refinement Loop"}]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "import anthropic\n\nclient = anthropic.Anthropic()\n\n\ndef call(prompt: str, max_tokens: int = 500, temperature: float = 0.0) -> str:\n    return client.messages.create(\n        model=\"claude-haiku-4-5-20251001\",\n        max_tokens=max_tokens,\n        temperature=temperature,\n        messages=[{\"role\": \"user\", \"content\": prompt}],\n    ).content[0].text.strip()\n\n\ndef evaluate(prompt: str, output: str, task_goal: str) -> dict:\n    \"\"\"\n    Ask the model to score the output and identify prompt weaknesses.\n    Returns a dict with 'score' (0-10) and 'critique'.\n    \"\"\"\n    eval_prompt = (\n        f\"Task goal: {task_goal}\\n\\n\"\n        f\"Prompt used:\\n{prompt}\\n\\n\"\n        f\"Output produced:\\n{output}\\n\\n\"\n        \"Score the output 0-10 for how well it meets the task goal. \"\n        \"Then list up to 3 specific weaknesses in the prompt that caused any shortfall. \"\n        \"Reply in this exact format:\\n\"\n        \"Score: <integer>\\n\"\n        \"Weaknesses:\\n- <weakness 1>\\n- <weakness 2>\\n- <weakness 3>\"\n    )\n    raw = call(eval_prompt, max_tokens=300)\n    score_line = next((l for l in raw.splitlines() if l.startswith(\"Score:\")), \"Score: 0\")\n    score = int(score_line.split(\":\")[1].strip())\n    critique = raw\n    return {\"score\": score, \"critique\": critique}\n\n\ndef refine_prompt(original_prompt: str, critique: str) -> str:\n    \"\"\"Rewrite the prompt to address the identified weaknesses.\"\"\"\n    return call(\n        f\"Here is a prompt and a critique of its weaknesses:\\n\\n\"\n        f\"Prompt:\\n{original_prompt}\\n\\n\"\n        f\"Critique:\\n{critique}\\n\\n\"\n        \"Rewrite the prompt to fix the weaknesses. Output ONLY the improved prompt text.\",\n        max_tokens=400,\n    )\n\n\ndef self_improving_prompt(\n    initial_prompt: str,\n    task_input: str,\n    task_goal: str,\n    target_score: int = 8,\n    max_iterations: int = 4,\n) -> dict:\n    prompt = initial_prompt\n    history = []\n\n    for i in range(max_iterations):\n        output = call(f\"{prompt}\\n\\nInput: {task_input}\")\n        evaluation = evaluate(prompt, output, task_goal)\n        history.append({\"iteration\": i + 1, \"prompt\": prompt, \"output\": output, **evaluation})\n\n        if evaluation[\"score\"] >= target_score:\n            break\n        prompt = refine_prompt(prompt, evaluation[\"critique\"])\n\n    return {\"final_prompt\": prompt, \"history\": history}\n\n\n# Example: start with a weak prompt and let the loop improve it\nresult = self_improving_prompt(\n    initial_prompt=\"Summarise this.\",\n    task_input=(\n        \"The product launch was delayed by two weeks due to supply chain issues. \"\n        \"Customer pre-orders have been notified. Engineering expects a fix by Q3.\"\n    ),\n    task_goal=(\n        \"A one-sentence executive summary that includes: what happened, impact on customers, \"\n        \"and the expected resolution timeline.\"\n    ),\n)\n\nfor step in result[\"history\"]:\n    print(f\"Iteration {step['iteration']} (score {step['score']}):\")\n    print(f\"  Prompt:  {step['prompt'][:100]}\")\n    print(f\"  Output:  {step['output'][:150]}\")\nprint(f\"\\nFinal prompt: {result['final_prompt']}\")\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Convergence and Stopping Conditions"}]
      },
      {
        "type": "table",
        "content": [
          {
            "type": "tableRow",
            "content": [
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Stopping condition"}]}]},
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "When to use"}]}]},
              {"type": "tableHeader", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Risk if omitted"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Score ≥ threshold"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Numeric quality can be defined for the task"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Loop continues past already-good prompts"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Max iteration cap"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Always — prevents unbounded cost"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Infinite loop if task has no achievable score"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Score plateau (no improvement in N rounds)"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "When ceiling is unknown"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Wastes calls on a prompt that cannot improve further"}]}]}
            ]
          },
          {
            "type": "tableRow",
            "content": [
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "External verifier passes"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Code generation, structured-output tasks with schema validation"}]}]},
              {"type": "tableCell", "content": [{"type": "paragraph", "content": [{"type": "text", "text": "Model score ≠ actual correctness"}]}]}
            ]
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Using an External Verifier Instead of Self-Score"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Self-scored evaluation suffers from the model's blind spots — if the model cannot produce good output, it may also misjudge quality. For structured tasks (JSON schema, passing tests, meeting a word count) an external verifier is more reliable."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "import anthropic\nimport json\n\nclient = anthropic.Anthropic()\n\n\ndef call(prompt: str, max_tokens: int = 400) -> str:\n    return client.messages.create(\n        model=\"claude-haiku-4-5-20251001\",\n        max_tokens=max_tokens,\n        temperature=0.0,\n        messages=[{\"role\": \"user\", \"content\": prompt}],\n    ).content[0].text.strip()\n\n\ndef verify_json_output(output: str, required_keys: list[str]) -> tuple[bool, str]:\n    \"\"\"External verifier: check that output is valid JSON with required keys.\"\"\"\n    try:\n        data = json.loads(output)\n        missing = [k for k in required_keys if k not in data]\n        if missing:\n            return False, f\"Missing keys: {missing}\"\n        return True, \"OK\"\n    except json.JSONDecodeError as e:\n        return False, f\"JSON parse error: {e}\"\n\n\ndef refine_for_json(\n    prompt: str, output: str, error_reason: str\n) -> str:\n    return call(\n        f\"The following prompt produced output that failed validation.\\n\"\n        f\"Prompt: {prompt}\\n\"\n        f\"Output: {output}\\n\"\n        f\"Error: {error_reason}\\n\\n\"\n        \"Rewrite the prompt so the model outputs ONLY valid JSON with these exact keys: \"\n        \"\\\"summary\\\", \\\"sentiment\\\", \\\"action_required\\\". Output ONLY the improved prompt.\"\n    )\n\n\nREQUIRED_KEYS = [\"summary\", \"sentiment\", \"action_required\"]\nprompt = \"Analyse this customer message and respond.\"\ntask_input = \"I've been waiting 2 weeks for my refund. This is unacceptable!\"\n\nfor iteration in range(5):\n    output = call(f\"{prompt}\\n\\nMessage: {task_input}\")\n    passed, reason = verify_json_output(output, REQUIRED_KEYS)\n    print(f\"Iter {iteration + 1}: {'PASS' if passed else 'FAIL'} — {reason}\")\n    if passed:\n        print(f\"Final output: {output}\")\n        break\n    prompt = refine_for_json(prompt, output, reason)\nelse:\n    print(\"Did not converge within 5 iterations.\")\n"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Keeping the Best Prompt Across Iterations"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Prompt refinement is not guaranteed to be monotonically improving. A rewrite that fixes one weakness can introduce another. Track the best-scoring prompt seen so far and return it rather than the last one."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "def self_improving_best_of(\n    initial_prompt: str,\n    task_input: str,\n    task_goal: str,\n    max_iterations: int = 5,\n) -> str:\n    \"\"\"\n    Run the refinement loop but return the best-scoring prompt seen,\n    not necessarily the last one.\n    \"\"\"\n    prompt = initial_prompt\n    best_prompt = prompt\n    best_score = -1\n\n    for _ in range(max_iterations):\n        output = call(f\"{prompt}\\n\\nInput: {task_input}\")\n        ev = evaluate(prompt, output, task_goal)\n\n        if ev[\"score\"] > best_score:\n            best_score = ev[\"score\"]\n            best_prompt = prompt\n\n        if best_score >= 9:\n            break\n\n        prompt = refine_prompt(prompt, ev[\"critique\"])\n\n    return best_prompt\n"
          }
        ]
      },
      {
        "type": "blockquote",
        "content": [
          {
            "type": "paragraph",
            "content": [
              {
                "type": "text",
                "text": "Self-improving prompts are most valuable during offline prompt development — not at inference time. Run the loop once to find a strong prompt, then hard-code that result for production. Using the refinement loop at inference time multiplies latency and cost by the number of iterations, which is almost never justified. The exception is code-generation tasks where an external verifier (test suite, type checker) provides fast, reliable feedback — there, adaptive refinement at runtime is a legitimate pattern."
              }
            ]
          }
        ]
      }
    ]
  }
}
