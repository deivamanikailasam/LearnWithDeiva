{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Approximating the Posterior" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "When the true posterior p(z|x) is intractable, variational inference replaces it with a simpler distribution q_φ(z|x) that is easy to sample from and evaluate. The variational distribution is chosen from a parametric family and its parameters φ are tuned to minimise the KL divergence from q to the true posterior — equivalently, to maximise the ELBO."
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "q^*_\\phi = \\underset{q_\\phi \\in \\mathcal{Q}}{\\arg\\min} \\; \\mathrm{KL}(q_\\phi(z \\mid x) \\| p(z \\mid x))"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The family Q must balance two competing pressures: it should be flexible enough to approximate the true posterior well, but tractable enough to compute the ELBO and its gradients."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Mean-Field Family" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The most common choice is the mean-field family: the variational distribution fully factorises over the latent dimensions:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "q_\\phi(z \\mid x) = \\prod_{i=1}^{d} q_{\\phi_i}(z_i \\mid x)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Each factor qᵢ has its own parameters φᵢ. For a Gaussian mean-field approximation, q_φ(z|x) = N(μ_φ(x), diag(σ²_φ(x))): a diagonal Gaussian with mean and variance that depend on x. This is the encoder in a VAE."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Amortised Variational Inference" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "In classical variational inference, the variational parameters φ are optimised separately for each data point x — this is expensive for large datasets. Amortised variational inference instead trains a neural network (the encoder/inference network) to predict φ as a function of x:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "(\\mu_\\phi(x),\\, \\sigma^2_\\phi(x)) = \\text{Encoder}_\\phi(x)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The same encoder is reused across all data points. At test time, encoding a new x requires one forward pass — no per-sample optimisation. The trade-off is the amortisation gap: a shared network cannot perfectly model every individual posterior, so the ELBO is slightly looser than per-sample VI."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": { "language": "python" },
        "content": [
          {
            "type": "text",
            "text": "import torch\nimport torch.nn as nn\n\nclass GaussianEncoder(nn.Module):\n    \"\"\"Amortised variational encoder: x → (μ_z, log σ²_z).\"\"\"\n    def __init__(self, x_dim, hidden_dim, z_dim):\n        super().__init__()\n        self.shared = nn.Sequential(\n            nn.Linear(x_dim, hidden_dim),\n            nn.ReLU(),\n            nn.Linear(hidden_dim, hidden_dim),\n            nn.ReLU(),\n        )\n        self.mu_head      = nn.Linear(hidden_dim, z_dim)\n        self.log_var_head = nn.Linear(hidden_dim, z_dim)\n\n    def forward(self, x):\n        h          = self.shared(x)\n        mu         = self.mu_head(h)\n        log_var    = self.log_var_head(h)\n        return mu, log_var\n\n    def sample(self, x):\n        \"\"\"Reparametrised sample z ~ q_φ(z|x).\"\"\"\n        mu, log_var = self.forward(x)\n        eps = torch.randn_like(mu)\n        return mu + torch.exp(0.5 * log_var) * eps\n\n# Usage\nenc = GaussianEncoder(x_dim=784, hidden_dim=512, z_dim=20)\nx   = torch.randn(64, 784)\nmu_z, log_var_z = enc(x)\nprint(mu_z.shape, log_var_z.shape)   # (64, 20), (64, 20)"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Reparametrisation Trick" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "To backpropagate gradients through a sample z ~ q_φ(z|x) = N(μ, σ²I), you cannot differentiate through the sampling step directly. The reparametrisation trick re-expresses z as a deterministic function of φ and a noise variable ε:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "z = \\mu_\\phi(x) + \\sigma_\\phi(x) \\odot \\varepsilon, \\quad \\varepsilon \\sim \\mathcal{N}(0, I)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Now z is differentiable with respect to φ: ∂z/∂μ = 1 and ∂z/∂σ = ε. Gradients flow back through z into the encoder parameters without needing to differentiate through the distribution itself. This works for any location-scale family (Gaussian, Laplace, logistic); for discrete latent variables, the Gumbel-Softmax trick provides a differentiable approximation."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": { "level": 2 },
        "content": [{ "type": "text", "text": "Posterior Collapse" }]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "A failure mode specific to variational inference is posterior collapse: q_φ(z|x) collapses to the prior p(z), so the encoder ignores x and the latent code carries no information. This happens when the decoder is powerful enough to reconstruct x from the prior distribution alone — the KL term in the ELBO is minimised at zero with no reconstruction cost."
          }
        ]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Common mitigations:"
          }
        ]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [{ "type": "paragraph", "content": [{ "type": "text", "marks": [{ "type": "bold" }], "text": "β-VAE:" }, { "type": "text", "text": " weight the reconstruction term higher: ELBO = E_q[log p(x|z)] − β KL with β < 1." }] }]
          },
          {
            "type": "listItem",
            "content": [{ "type": "paragraph", "content": [{ "type": "text", "marks": [{ "type": "bold" }], "text": "KL annealing:" }, { "type": "text", "text": " start with β=0 and linearly increase to 1 during training." }] }]
          },
          {
            "type": "listItem",
            "content": [{ "type": "paragraph", "content": [{ "type": "text", "marks": [{ "type": "bold" }], "text": "Free bits:" }, { "type": "text", "text": " allow a fixed number of KL nats \"for free\" before penalising — each latent dimension is not penalised until its KL exceeds a threshold λ." }] }]
          },
          {
            "type": "listItem",
            "content": [{ "type": "paragraph", "content": [{ "type": "text", "marks": [{ "type": "bold" }], "text": "Skip connections in decoder:" }, { "type": "text", "text": " constraining the decoder capacity forces it to use z." }] }]
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": { "language": "python" },
        "content": [
          {
            "type": "text",
            "text": "def elbo_with_free_bits(recon_loss, mu, log_var, free_bits=0.5):\n    \"\"\"\n    ELBO loss with per-dimension free-bits regularisation.\n    Each latent dim is only penalised once its KL exceeds `free_bits` nats.\n    \"\"\"\n    # Per-dimension KL: (batch, z_dim)\n    kl_per_dim = 0.5 * (log_var.exp() + mu**2 - 1 - log_var)\n    # Threshold: allow free_bits per dimension\n    kl_per_dim = torch.clamp(kl_per_dim, min=free_bits)\n    kl = kl_per_dim.sum(dim=1).mean()\n    return recon_loss + kl"
          }
        ]
      }
    ]
  }
}
