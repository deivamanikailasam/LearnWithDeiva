{
  "format": "tiptap/v1",
  "doc": {
    "type": "doc",
    "content": [
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Real-valued Non-Volume Preserving (RealNVP, Dinh et al., 2017) is the architecture that made normalising flows practical for high-dimensional image generation. Its core innovation is the affine coupling layer — a design that achieves simultaneously fast forward (generation), fast inverse (encoding), and O(d) log-determinant computation by splitting the input and applying an unconstrained neural network to only half of it. Understanding the coupling layer design, its masking strategies, and the multi-scale architecture that enables generation at image resolution is essential for reading all modern flow-based models."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "NICE: The Origin of Coupling Layers"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "NICE (Non-linear Independent Components Estimation, Dinh et al., 2015) introduced the additive coupling layer as the simplest bijection with a tractable Jacobian. Given input x split into two halves x₁, x₂:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "y_1 = x_1, \\quad y_2 = x_2 + m(x_1)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "m can be any neural network — no invertibility constraint. The Jacobian is lower triangular with 1s on the diagonal: det J = 1, log-det = 0. This is volume-preserving — NICE relies entirely on the final scale layer and additive shifts to reshape the distribution. The limitation: no learnable scaling means the model cannot easily expand or compress probability mass."
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "RealNVP: Affine Coupling with Learnable Scaling"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "RealNVP replaces additive-only coupling with an affine (scale + shift) transform:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "y_1 = x_1, \\quad y_2 = x_2 \\odot \\exp(s(x_1)) + t(x_1)"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Now the Jacobian's lower-right block is diag(exp(s(x₁))), giving log|det J| = ∑ᵢ s(x₁)ᵢ — still O(d), but non-zero. The scale function s is learned by the same network as the shift t (just different output heads). Inversion is as easy as forward:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "x_1 = y_1, \\quad x_2 = (y_2 - t(y_1)) \\odot \\exp(-s(y_1))"
        }
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n\ndef make_st_net(d_in: int, d_out: int, hidden: int = 256) -> nn.Module:\n    \"\"\"Scale-and-translation network for coupling layer.\"\"\"\n    return nn.Sequential(\n        nn.Linear(d_in, hidden), nn.ReLU(),\n        nn.Linear(hidden, hidden), nn.ReLU(),\n        nn.Linear(hidden, 2 * d_out),   # first half = s, second half = t\n    )\n\n\nclass AffineCoupling(nn.Module):\n    \"\"\"\n    RealNVP affine coupling layer.\n    Splits the input in half; the first half conditions the affine transform on the second.\n    Alternating layers swap which half is conditioned vs transformed.\n    \"\"\"\n    def __init__(self, d: int, hidden: int = 256, swap: bool = False):\n        super().__init__()\n        self.d1  = d // 2\n        self.d2  = d - self.d1\n        self.swap = swap  # swap which half is conditioned\n        self.net = make_st_net(\n            d_in  = self.d2 if swap else self.d1,\n            d_out = self.d1 if swap else self.d2,\n            hidden = hidden,\n        )\n\n    def _split(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:\n        x1, x2 = x[:, :self.d1], x[:, self.d1:]\n        return (x2, x1) if self.swap else (x1, x2)\n\n    def _merge(self, cond: torch.Tensor, out: torch.Tensor) -> torch.Tensor:\n        return torch.cat([out, cond] if self.swap else [cond, out], dim=-1)\n\n    def forward(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:\n        cond, transformed = self._split(x)\n        st = self.net(cond)             # (B, 2*d_transformed)\n        s, t = st.chunk(2, dim=-1)\n        s = torch.tanh(s)              # bound scale for stability\n        y_out = transformed * torch.exp(s) + t\n        log_det = s.sum(-1)            # (B,)\n        return self._merge(cond, y_out), log_det\n\n    def inverse(self, y: torch.Tensor) -> torch.Tensor:\n        cond, transformed = self._split(y)\n        st = self.net(cond)\n        s, t = st.chunk(2, dim=-1)\n        s = torch.tanh(s)\n        x_out = (transformed - t) * torch.exp(-s)\n        return self._merge(cond, x_out)"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Masking Strategies for Images"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "For images, splitting along a channel dimension (first half of channels vs second) loses spatial structure. RealNVP uses two masking patterns applied on the flattened pixel grid:"
          }
        ]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {
                    "type": "text",
                    "marks": [{"type": "bold"}],
                    "text": "Checkerboard mask:"
                  },
                  {
                    "type": "text",
                    "text": " Pixels at positions (i,j) with (i+j) % 2 == 0 are conditioned; the rest are transformed. Alternating layers flip the mask. This ensures each pixel is eventually conditioned on its neighbours."
                  }
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {
                    "type": "text",
                    "marks": [{"type": "bold"}],
                    "text": "Channel mask:"
                  },
                  {
                    "type": "text",
                    "text": " After a squeeze operation (reshaping H×W into H/2×W/2 with 4× channels), split along the channel dimension. This is applied at lower spatial resolutions in the multi-scale architecture."
                  }
                ]
              }
            ]
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "def checkerboard_mask(H: int, W: int, invert: bool = False) -> torch.Tensor:\n    \"\"\"\n    Boolean mask of shape (1, 1, H, W): True at (i,j) where (i+j) % 2 == 0.\n    Set invert=True for alternating layers.\n    \"\"\"\n    i = torch.arange(H).unsqueeze(1)   # (H, 1)\n    j = torch.arange(W).unsqueeze(0)   # (1, W)\n    mask = ((i + j) % 2 == 0).unsqueeze(0).unsqueeze(0)   # (1, 1, H, W)\n    return ~mask if invert else mask\n\n\nclass ImageAffineCoupling(nn.Module):\n    \"\"\"RealNVP coupling layer for images with checkerboard or channel masking.\"\"\"\n    def __init__(self, C: int, H: int, W: int, mask_type: str = \"checkerboard\", invert: bool = False):\n        super().__init__()\n        if mask_type == \"checkerboard\":\n            self.register_buffer(\"mask\", checkerboard_mask(H, W, invert).float())\n            net_in = C   # all channels, but only unmasked positions are active\n        else:\n            # Channel mask: first C//2 channels are conditioned\n            mask = torch.zeros(1, C, H, W)\n            mask[:, :C//2] = 1.0\n            self.register_buffer(\"mask\", mask if not invert else (1 - mask))\n            net_in = C // 2\n\n        # Scale-and-translation: conditioned channels -> (s, t) for transformed channels\n        self.net = nn.Sequential(\n            nn.Conv2d(C, 64, 3, padding=1), nn.ReLU(),\n            nn.Conv2d(64, 64, 1), nn.ReLU(),\n            nn.Conv2d(64, 2 * C, 3, padding=1),\n        )\n        # Initialise last conv to zero so identity transform at start of training\n        nn.init.zeros_(self.net[-1].weight)\n        nn.init.zeros_(self.net[-1].bias)\n\n    def forward(self, x: torch.Tensor) -> tuple[torch.Tensor, torch.Tensor]:\n        m = self.mask\n        x_cond = x * m                   # masked (conditioned) part\n        st = self.net(x_cond)            # (B, 2C, H, W)\n        s, t = st.chunk(2, dim=1)\n        s = torch.tanh(s) * (1 - m)     # only transform the unmasked positions\n        t = t * (1 - m)\n        y = x_cond + (1 - m) * (x * torch.exp(s) + t)\n        log_det = s.sum(dim=(1, 2, 3))  # (B,)\n        return y, log_det\n\n    def inverse(self, y: torch.Tensor) -> torch.Tensor:\n        m = self.mask\n        y_cond = y * m\n        st = self.net(y_cond)\n        s, t = st.chunk(2, dim=1)\n        s = torch.tanh(s) * (1 - m)\n        t = t * (1 - m)\n        return y_cond + (1 - m) * ((y - t) * torch.exp(-s))"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Multi-Scale Architecture"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "At full image resolution (e.g. 32×32×3 = 3072 dims), a coupling layer's s/t network must handle all 3072 dimensions at once. RealNVP adds a squeeze-and-split structure to reduce this cost:"
          }
        ]
      },
      {
        "type": "orderedList",
        "content": [
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {
                    "type": "text",
                    "marks": [{"type": "bold"}],
                    "text": "Squeeze:"
                  },
                  {
                    "type": "text",
                    "text": " Reshape (H, W, C) → (H/2, W/2, 4C) by rearranging non-overlapping 2×2 spatial patches into channels. This is an invertible permutation with log-det = 0."
                  }
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {
                    "type": "text",
                    "marks": [{"type": "bold"}],
                    "text": "Apply coupling layers"
                  },
                  {
                    "type": "text",
                    "text": " at the reduced spatial resolution."
                  }
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {
                    "type": "text",
                    "marks": [{"type": "bold"}],
                    "text": "Split:"
                  },
                  {
                    "type": "text",
                    "text": " Factor out half the channels as latent variables (z₁) and continue with the other half. These factored-out variables are directly modelled by the prior — they don't need to be processed by further layers. This dramatically reduces the dimension at each scale."
                  }
                ]
              }
            ]
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "def squeeze(x: torch.Tensor) -> torch.Tensor:\n    \"\"\"\n    Squeeze: (B, C, H, W) -> (B, 4C, H/2, W/2) by rearranging 2x2 patches.\n    Invertible with log-det = 0.\n    \"\"\"\n    B, C, H, W = x.shape\n    assert H % 2 == 0 and W % 2 == 0\n    x = x.reshape(B, C, H//2, 2, W//2, 2)\n    x = x.permute(0, 1, 3, 5, 2, 4)    # (B, C, 2, 2, H/2, W/2)\n    return x.reshape(B, C * 4, H//2, W//2)\n\n\ndef unsqueeze(x: torch.Tensor) -> torch.Tensor:\n    \"\"\"Inverse of squeeze.\"\"\"\n    B, C4, H2, W2 = x.shape\n    C = C4 // 4\n    x = x.reshape(B, C, 2, 2, H2, W2)\n    x = x.permute(0, 1, 4, 2, 5, 3)    # (B, C, H/2, 2, W/2, 2)\n    return x.reshape(B, C, H2 * 2, W2 * 2)\n\n\nclass MultiScaleRealNVP(nn.Module):\n    def __init__(self, C: int = 3, H: int = 32, W: int = 32, n_scales: int = 3, n_coupling: int = 4):\n        super().__init__()\n        self.scales = nn.ModuleList()\n        self.splits = []   # (H_i, W_i, C_split_i) for each scale\n\n        c, h, w = C, H, W\n        for scale in range(n_scales):\n            # One scale: squeeze + n coupling layers\n            scale_layers = nn.ModuleList([\n                ImageAffineCoupling(c * 4, h//2, w//2,\n                                    mask_type=\"channel\", invert=(i % 2 == 1))\n                for i in range(n_coupling)\n            ])\n            self.scales.append(scale_layers)\n            # After processing: split out half the channels\n            c_remaining = (c * 4) // 2\n            self.splits.append((h//2, w//2, c * 4 - c_remaining))\n            c, h, w = c_remaining, h//2, w//2\n\n    def forward(self, x):\n        log_det_total = torch.zeros(x.size(0), device=x.device)\n        z_list = []\n\n        for scale_idx, scale_layers in enumerate(self.scales):\n            x = squeeze(x)\n            for layer in scale_layers:\n                x, ld = layer(x)\n                log_det_total = log_det_total + ld\n            # Factor out half the channels\n            C_half = x.size(1) // 2\n            z_list.append(x[:, :C_half])   # factored out\n            x = x[:, C_half:]              # continue processing\n\n        z_list.append(x)   # final residual\n        return z_list, log_det_total"
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Dequantization: Handling Discrete Pixel Values"}]
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "Image pixel values are integers in [0, 255]. A continuous flow on discrete data assigns all probability mass to isolated points (measure zero), so the model degenerates. The fix is dequantization: add uniform noise u ~ U[0,1) to each pixel before training, converting integers to continuous values in [0, 256). The training objective becomes:"
          }
        ]
      },
      {
        "type": "blockMath",
        "attrs": {
          "latex": "\\log p_{\\text{data}}(x) \\geq \\mathbb{E}_{u \\sim U[0,1)^d}\\bigl[\\log p_{\\text{flow}}(x + u)\\bigr]"
        }
      },
      {
        "type": "paragraph",
        "content": [
          {
            "type": "text",
            "text": "The left side is the intractable discrete likelihood; the right side is a lower bound computable with the flow. In practice, also divide by 256 so the normalised pixel values lie in [0, 1). Bits-per-dimension (BPD) = −(log₂ likelihood) / d is the evaluation metric."
          }
        ]
      },
      {
        "type": "codeBlock",
        "attrs": {"language": "python"},
        "content": [
          {
            "type": "text",
            "text": "def preprocess_images(\n    x: torch.Tensor,   # (B, C, H, W) uint8 in [0, 255]\n    n_bits: int = 8,\n    dequantize: bool = True,\n) -> tuple[torch.Tensor, float]:\n    \"\"\"\n    Prepare images for flow training:\n    1. Add uniform dequantization noise.\n    2. Normalise to [0, 1).\n    Returns (x_preprocessed, dequant_log_prob_correction).\n    \"\"\"\n    x = x.float()\n    n_bins = 2 ** n_bits          # 256 for 8-bit images\n    if dequantize:\n        x = x + torch.rand_like(x)  # U[0,1) uniform noise\n    x = x / n_bins               # normalise to [0, 1)\n\n    # The dequantization correction: subtracting log(n_bins^d) = -d * log(n_bins)\n    # We add this constant to the log-likelihood to report correct BPD\n    d = x[0].numel()\n    log_correction = -d * torch.log(torch.tensor(float(n_bins)))\n    return x, log_correction.item()\n\n\ndef bits_per_dim(\n    log_likelihood: torch.Tensor,  # (B,) in nats\n    d: int,\n    log_correction: float = 0.0,\n) -> float:\n    return -(log_likelihood.mean().item() + log_correction) / (d * 0.693147)  # / log(2)"
          }
        ]
      },
      {
        "type": "blockquote",
        "content": [
          {
            "type": "paragraph",
            "content": [
              {
                "type": "text",
                "marks": [{"type": "bold"}],
                "text": "Mental model."
              },
              {
                "type": "text",
                "text": " Think of the coupling layer as a one-way valve: the left half of the input opens the valve for the right half, controlling how much it expands or contracts. The key insight is that during inversion you still know the left half (it passed through unchanged), so you can re-compute the valve position and reverse the transform exactly. The alternating swap of which half controls the valve ensures every dimension eventually influences and is influenced by every other — the full dependency structure is captured across layers even though each individual layer only updates half."
              }
            ]
          }
        ]
      },
      {
        "type": "heading",
        "attrs": {"level": 2},
        "content": [{"type": "text", "text": "Key Takeaways"}]
      },
      {
        "type": "bulletList",
        "content": [
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {
                    "type": "text",
                    "text": "NICE uses additive coupling (volume-preserving, log-det = 0); RealNVP extends to affine coupling (scale + shift, log-det = ∑ sᵢ) enabling learnable density changes."
                  }
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {
                    "type": "text",
                    "text": "Coupling layers achieve O(d) log-det because the Jacobian is triangular — the first half is the identity and the second half is diagonal diag(exp(s(x₁)))."
                  }
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {
                    "type": "text",
                    "text": "For images, checkerboard masking preserves spatial structure by conditioning on alternating pixels; channel masking is used after squeeze operations in the multi-scale architecture."
                  }
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {
                    "type": "text",
                    "text": "The multi-scale architecture (squeeze → coupling → split) progressively halves spatial resolution while factoring out latent variables at each scale — reducing the dimension processed at each level."
                  }
                ]
              }
            ]
          },
          {
            "type": "listItem",
            "content": [
              {
                "type": "paragraph",
                "content": [
                  {
                    "type": "text",
                    "text": "Dequantization adds U[0,1) noise before training to convert discrete pixel values to continuous densities; bits-per-dimension (BPD) is the standard evaluation metric for image flows."
                  }
                ]
              }
            ]
          }
        ]
      }
    ]
  }
}
