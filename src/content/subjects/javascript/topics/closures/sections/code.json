{
  "snippets": [
    {
      "title": "Private state with a closure",
      "language": "javascript",
      "code": "function createWallet(initial = 0) {\n  let balance = initial; // private — no outside access\n  return {\n    deposit: (amount) => (balance += amount),\n    getBalance: () => balance,\n  };\n}\n\nconst wallet = createWallet(100);\nwallet.deposit(50);\nwallet.getBalance(); // 150\n// wallet.balance is undefined — truly private",
      "explanation": "The balance variable is only reachable through the returned methods."
    }
  ]
}
